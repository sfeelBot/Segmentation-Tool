# QA — 버그 및 VOC 추적

## 2026-08-28 — installer 시작 대기 안내 (Closed)

- QApplication과 splash를 무거운 AI/그래프 라이브러리보다 먼저 생성해 즉시 준비 상태 표시.
- numpy/OpenCV/Pillow, PyTorch, matplotlib, 사용자 환경, 프로젝트 화면 준비 단계를 순서대로 안내.
- 접근 권한이 사라진 최근 프로젝트 경로는 건너뛰어 시작 창 중단을 방지.
- splash 단위 검증과 release 회귀 테스트 22개, `py_compile`, `diff --check` 통과.

## 2026-08-28 — 추론 탭 UI 병목 후속 (Closed)

- 원인: opacity/threshold 값 변경마다 UI 스레드에서 전체 해상도 재필터링·오버레이 합성을 실행하고, 픽스맵 갱신마다 줌/패닝을 초기화함.
- 조치: opacity는 기존 필터 결과를 재사용하고, opacity 75ms/threshold 150ms debounce를 적용했으며 같은 이미지의 픽스맵 갱신은 현재 뷰를 보존함. reject가 없는 기본 threshold에서는 `class_map` 중복 복사를 생략함.
- 2000×1500 합성 입력 계측: opacity 처리 중앙값 357.3ms(`refilter`) → 210.9ms(`reblend`), 연속 20 tick은 20회 → 1회 실행.
- 회귀 검증: `tests/test_github_issue_23.py` 7건 통과.

## 2026-08-28 — 추론 오버레이 불투명도 배경 밝기 (Closed)

- 증상: 불투명도를 높이면 배경 클래스(`0`)도 검정색과 혼합되어 원본 이미지 전체가 어두워짐.
- 조치: 공통 합성 경로에서 배경은 원본 픽셀을 보존하고 전경 검출 영역에만 opacity를 적용.
- 회귀 검증: `tests/test_github_issue_23.py` 3건 통과(배경 RGB 불변, 전경 50% 혼합값 포함).

## Open Issues

| ID | 우선순위 | 설명 | 보고자 | 상태 |
|----|----------|------|--------|------|
| BUG-004 | P3 | `auto_labeler.collect_unlabeled()`의 `get_label_status()` 1회 read 통합(R6)이 `get_ok()+load()` 2회 read 구버전과 100% 동치는 아님 — `annotations` 배열에 앱이 절대 생성하지 않는(=`save()`가 쓰지 않는) 미인식 `type` 값의 원소만 있는 손상/외부편집 JSON의 경우, 구버전은 `load()`가 해당 원소를 조용히 건너뛰어 결과 리스트가 비므로 "unlabeled"로 간주(자동 라벨링 대상에 포함)했지만 신버전은 `annotations` 배열 자체가 non-empty이므로 "labeled"로 간주(대상에서 제외). 정상 사용(앱이 직접 저장한 JSON)에서는 재현 불가 — 손으로 편집했거나 외부 도구가 만든 손상 파일에서만 발생. 부수 관찰: 최상위가 dict가 아닌(JSON 배열 등) 손상 파일에서는 구버전(`load()`가 `AttributeError`로 크래시)과 달리 신버전은 크래시 없이 "unlabeled"로 안전 처리 — 이 케이스는 오히려 신버전이 더 견고함 | R6 검증(verifier) | Open |
| BUG-028 | P3 | `pytest tests/`를 한 프로세스에서 전체 실행하면 특정 파일 조합(예: `test_config_form_no_wheel.py` → `test_github_issue_23.py` 순서로 수집)에서 `ImportError: DLL load failed while importing QtSvg`로 시작해 종국에는 `Windows fatal exception: code 0xc0000139`(STATUS_ENTRYPOINT_NOT_FOUND)로 수집 자체가 인터럽트됨. 개별 파일은 전부(12개 파일, 77개 테스트) 단독 실행 시 100% 통과 — 실행 순서 의존적인 프로세스 전역 DLL 충돌. 근본 원인: 일부 테스트 파일이 모듈 최상단에서 `QApplication(sys.argv)`를 즉시 생성(예: `test_annotation_type_merge.py:14`)하는데, 그 뒤 다른 파일이 `torch`를 임포트(`app.core.trainer` 경유)한 **이후에** `from PyQt6 import QtSvg`를 임포트하면 실패함(최소 재현: `QApplication` 생성 → `torch` 임포트 → `QtSvg` 임포트 3줄이면 pytest 없이도 100% 재현) — PyQt6 6.7.1 + PyTorch 2.11.0+cu128 조합에서 두 라이브러리가 번들한 동명 네이티브 DLL(zlib/libpng류 추정) 버전 충돌로 보임. **실제 앱(`python main.py`)은 영향 없음** — `main.py`가 항상 `torch`를 최상단에서 먼저 임포트한 뒤에야 `QApplication`을 생성하므로 이 순서 문제가 발생하지 않음을 별도로 확인함(동일 3줄 순서를 main.py 순서로 바꾸면 에러 없음). CI/전체 회귀 스위트 신뢰성에만 영향 | 통합 회귀 검증(verifier, 2026-08-29) — pytest 전체 스위트 실행 중 발견 | Open |

---

## Closed Issues

| ID | 수정 버전 | 설명 | 근본 원인 | 해결 방법 |
|----|-----------|------|-----------|-----------|
| BUG-003 | 2026-08-21 버그 일괄정리 | `AnnotationCanvas._translate_selected()`(Select 도구로 어노테이션을 캔버스 밖까지 드래그) 후 마스크가 전량 0이 되어도 정리가 없어 다음 이미지 전환 전까지 렌더링 안 되는 유령 항목("#N [Mask] ...")이 어노테이션 목록에 남을 수 있음 | 이동 완료 처리에서 결과 마스크가 비었는지 확인하는 로직이 없었음 | `_translate_selected()` 끝에서 이동 후 마스크가 전량 0인 brush_mask 어노테이션을 `self._annotations`와 `self._selected_ids`에서 즉시 제거하도록 필터링 추가(폴리곤 타입은 마스크가 없어 무관, 대상에서 자연히 제외). 커밋 `251608e`. **재검증 완료(2026-08-21, verifier)** — `AnnotationCanvas` 단독 인스턴스에 `QTest.mousePress/mouseMove/mouseRelease`로 실제 브러시 스트로크 생성 후 Select 도구로 선택→위젯 경계 밖까지 실제 드래그 이벤트로 이동, `_annotations`/`_selected_ids`에 빈 마스크 유령 항목 0건 확인. |
| BUG-014 | 2026-08-21 버그 일괄정리 | `AnnotationCanvas._push_undo()`가 매 브러시 스트로크 시작마다 `self._annotations` 전체를 `copy.deepcopy()`하는데, 대형 이미지+brush_mask 수백 개 상태에서는 이 deepcopy 자체가 `MemoryError`(서브클래스인 `numpy._core._exceptions._ArrayMemoryError` 포함)로 앱을 크래시시킬 수 있음(실측 재현됨) | 어노테이션당 전체 해상도 dense 마스크 저장 구조가 근본 원인(구조적 한계, 이번엔 재설계하지 않음) | 근본 재설계 대신 안전장치만 추가 — `_push_undo()`의 `copy.deepcopy()` 호출을 `try/except MemoryError`로 감싸, 실패 시 로그 경고만 남기고 해당 undo 스텝을 건너뛴 채(스택에 추가 안 함) 편집 작업 자체는 계속 진행되게 함. "이번 동작은 undo 불가"가 "앱이 죽어 직전 작업이 전부 날아감"보다 훨씬 나은 결과. `numpy`의 `_ArrayMemoryError`는 `MemoryError` 서브클래스라 버전별 private import 불필요. 커밋 `251608e`. **재검증 완료(2026-08-21, verifier)** — `git show`로 `try/except MemoryError` 구조 확인. 실제 OOM은 재현하지 않음(범위 밖으로 판단). 회귀 확인: 실제 브러시 스트로크 2회 후 `canvas.undo()` 정상 범위 호출 시 정확히 이전 상태로 복귀, 정상 케이스 영향 없음. |
| BUG-009 | 2026-08-21 버그 일괄정리 | 추론 탭 `InferenceImageList`에서 검색 필터를 지우면 트리는 전체 이미지로 복원되지만 하단 이전/다음 카운터("N / M")가 갱신 안 되고 필터 중 값에 고정돼 남음 — "다음" 버튼으로 실제 네비게이션이 일어나야만 정정됨(데이터 손상 없음, 표시만 오류) | `InferenceImageList._apply_display()`가 필터/정렬 변경 후 선택 경로가 이전과 동일하면 `image_selected` 시그널을 재발행하지 않아, `inference_tab.py`의 `_update_nav_label()` 호출 지점(시그널 수신 시/이전·다음 클릭 시)이 "같은 이미지 선택 유지+전체 개수만 변경"되는 케이스를 놓침 | `InferenceImageList`에 `display_changed` 시그널을 신설해 `_apply_display()` 끝에서 선택 변경 여부와 무관하게 항상 emit, `inference_tab.py`가 이 시그널에 `_update_nav_label`을 직접 연결해 필터/정렬로 표시 목록이 바뀌는 모든 경로에서 카운터가 즉시 갱신되게 함. 커밋 `251608e`. **재검증 완료(2026-08-21, verifier)** — `InferenceTab` 실제 인스턴스에서 임시 폴더(하위 폴더 포함 3장) 로드 후 검색창에 실제 키 입력으로 필터링("1/1")→해제 시 "다음" 버튼 없이도 카운터가 즉시 "1/3"으로 정정됨을 확인. |
| BUG-010 | 2026-08-21 버그 일괄정리 | `InferenceImageList`의 폴더 트리에서 하위 항목이 있는 폴더 헤더 옆에 펼침/접힘 화살표가 전혀 렌더링되지 않음(더블클릭은 정상 동작하나 발견성 낮음) | `_TREE_STYLE`의 `QTreeWidget::branch { background: ... }` 규칙이 Qt의 OS 기본 화살표 렌더링을 억제하는데 상태별 커스텀 화살표를 지정하지 않았음(`app/widgets/image_browser.py`의 기존 스타일을 그대로 재사용한 결과) | 커스텀 화살표 이미지를 새로 만드는 대신, `inference_image_list.py`의 `_TREE_STYLE` 사본에서 `QTreeWidget::branch { background: ... }` 규칙 자체를 삭제해 Qt 기본 화살표 렌더링이 복원되게 함(`image_browser.py` 쪽 스타일은 BUG-005로 해당 코드 자체가 제거돼 무관해짐). 커밋 `251608e`. **재검증 완료(2026-08-21, verifier, 조건부)** — 런타임에서 `_TREE_STYLE`에 `QTreeWidget::branch` 규칙이 실제로 없음을 재확인, 정렬 "폴더" 전환 후 하위 폴더 항목 펼침/접힘 토글 및 트리 인디케이터 영역 실제 클릭 시 토글 정상 동작 확인. 다만 이 headless(offscreen) 환경 특성상 화살표 아이콘이 실제로 "보이는지" 픽셀 단위 육안 확인은 못 함 — 원인(스타일 규칙) 제거는 확실히 확인했으므로 최종 시각 확인은 사용자 환경에서의 추가 확인을 권장. |
| BUG-005 | 2026-08-21 죽은 코드 정리 | `image_browser.py`의 폴더 그룹핑 기능(`_build_folder_tree`/`_make_folder_item`/정렬모드 "폴더")이 실제로는 어떤 사용자 동작으로도 트리거되지 않는 죽은 코드 상태(`reload()`는 비재귀 스캔만 하고 이미지 추가 경로도 전부 평탄 복사라 하위폴더 구조 자체가 만들어지지 않음) | 진짜로 재귀 폴더 스캔 기능을 만들려면 `app/core/dataset.py`의 학습 스캔 로직까지 함께 바꿔야 함(2026-05-27 `00fd6779`가 `image_browser.py`를 의도적으로 비재귀로 맞춘 이유가 "학습 시 제외되는 이미지가 브라우저엔 보이는 불일치 방지"였기 때문) — 이는 "버그 수정" 범위를 넘는 별도 기능 개발이라 판단, 대신 도달 불가능한 죽은 코드를 제거하는 방향으로 결정(`docs/decisions-needed.md` 등록 후 리더 판단) | `image_browser.py`에서 `_build_folder_tree()`/`_make_folder_item()`/`_get_folder_font()`/모듈 전역 `_folder_font`/`from collections import defaultdict` 삭제, 정렬모드 목록에서 "folder" 제거(`_SORT_MODE_KEYS`, i18n `browser.sort.folder` ko/en 키 삭제), `_apply_display()`의 "folder" 정렬 분기와 트리 분기 삭제, `_select_by_index()`의 폴더 자동펼침 로직·`refresh_item()`의 폴더 자식 분기(둘 다 폴더 헤더 자식(`item.parent() is not None`)이 더 이상 존재하지 않아 항상 동일 분기만 타므로 단순화) 제거. 추론 탭 `inference_image_list.py`는 `rglob()` 기반 실제 재귀 스캔을 쓰는 별개 구현이라 살아있는 코드로 확인, 그대로 유지. GitHub #1 VOC 응답과 로드맵 "폴더 접기/펼치기 그룹 헤더" 완료 표시를 "라벨링 탭은 폴더 그룹핑 미지원(단일 평탄 목록) — 필요 시 별도 기능 요청으로 논의"로 정정. **재검증 완료(2026-08-21, verifier)** — `projects/nok` 스크래치 사본으로 `ImageBrowser` 실제 인스턴스화, 정렬 콤보 4개(폴더 옵션 없음, 의도된 변경) 확인 + 회귀 확인(4개 정렬 모드 전환, 검색 필터/해제, 이미지 수동 추가/삭제 후 `reload()`, `refresh_item()`) 전부 정상. 원본 `projects/nok` 무변경. |
| BUG-006 | 2026-08-21 버그 일괄정리 | `model_tab.py` 검증/로드 경로 불일치 — `model_validator.validate()`가 허용 목록 밖 import를 [WARN]+`ok=True`로만 표시해 검증 통과·로드버튼 활성화로 보이지만, 실제 `load_from_code()`의 exec 샌드박스는 같은 import를 무조건 `ImportError`로 실패시킴 | 검증기와 로더의 허용 정책이 서로 다른 엄격도로 구현돼 있었음(검증기는 경고, 로더는 하드 차단) | 보안 샌드박스(로더)는 완화하지 않고 검증기를 로더 실동작에 맞춰 엄격화 — `model_validator.py`의 `_check_imports()`에서 허용목록 밖 import를 `warnings`가 아닌 `errors`에 추가(`ok=False`)하도록 2줄 수정. `model_tab.py`는 이미 `result.ok`로 로드버튼을 제어해 추가 수정 불필요. 커밋 `7e95ee0`. **재검증 완료(2026-08-21, verifier)** — `ModelTab` 실제 인스턴스에서 `import random` 포함 코드로 검증 버튼 실제 클릭 시 "✗ 검증 실패"+로드 버튼 비활성화 확인, 허용 모듈만 쓰는 실제 프리셋(`simple_unet`)으로는 검증 통과+로드 버튼 클릭까지 실제 로드 성공(회귀 없음) 확인. |
| BUG-001 | Phase 1 | `ImportError: DLL load failed while importing QtWidgets` — PyQt6 6.11.0 기동 불가 | PyQt6 6.8+ 가 Windows Anaconda(Python 3.13) 환경에서 DLL 프로시저 못 찾음 | PyQt6 6.7.1 고정 (`requirements.txt`); 6.8+ 설치 금지 |
| BUG-024 | Installer 1.10.1 | 설치본 실행 시 PyQt6 누락 또는 Torch `c10.dll` WinError 1114 | 빌드 Python 불일치 및 frozen Windows에서 Qt가 CUDA Torch보다 먼저 초기화됨 | 단일 Python 전체 의존성 사전검사, PyQt5/PySide 및 개발 패키지 제외, Torch 선행 로드. 1.815GB 설치본 무인 설치 및 20초 기동 통과 |
| BUG-025 | GitHub #27 | 일반 PowerShell에서 `build.bat`이 `WindowsApps\\python.exe`를 선택해 즉시 실패 | `where python.exe` 첫 결과를 검증 전 채택 | `py -3.12` 시스템 Python 우선 탐색, WindowsApps 별칭 제외, 패키지 설치 명령 안내 |
| BUG-002 | R1(미배포, 커밋 예정) | `annotation_store.rle_encode()` — 모든 brush_mask 어노테이션이 저장 시 빈 RLE 문자열이 되어 전량 데이터 유실 (재현: `mask[10:20,10:20]=1` 인 100픽셀 마스크도 `rle_encode()` → `""`, 왕복 후 `rle_decode()` 결과 전부 0). `projects/nok/` 실데이터는 전부 polygon 타입이라 이 프로젝트에서는 아직 미발현. 폴리곤 어노테이션은 영향 없음(이 함수를 타지 않음). | `rle_encode()` 내부 `diff = np.diff(flat_uint8, prepend=0, append=0)` 가 `flat`의 uint8 dtype 을 그대로 유지해 1→0 하강 엣지(-1)가 부호없는 정수 언더플로우로 255 가 됨 → `np.where(diff == -1)` 이 항상 빈 배열 → `n = min(len(starts), len(ends)) = 0` → `return ""`. v1.6.0 초기 릴리즈부터 존재하던 결함으로 추정(커밋 43fad19), 이후 경쟁조건 방지 커밋(5769306)의 `n = min(...)` 방어 코드가 예외를 삼켜 증상을 더 눈에 띄지 않게 만듦. | `np.diff` 호출 전 `flat`을 `int8`로 캐스팅(`flat.astype(np.int8)`, prepend/append도 `np.int8(0)`)해 부호없는 언더플로우를 방지. 기존 `n = min(len(starts), len(ends))` 방어 코드(경쟁조건 대비)는 그대로 유지 — 별개 안전장치. 100px/~1.8Mpx 등 여러 마스크로 `rle_encode`→`rle_decode` 라운드트립 검증 완료. **주의**: 이 수정 이전에 이미 저장되어 `"rle": ""`로 남은 과거 brush_mask 데이터는 복구 불가(원본 마스크가 저장 시점에 이미 소실됨). |
| BUG-007 | 디자인 톤 재검토 라운드4 | `loss_chart.py` 배색 정규화(커밋 `e76361a`)에서 그리드(`_GRID`)와 epoch 경계 세로선(`_EPOCH`)이 동일한 `#374151`로 통일돼 두 선이 실제 렌더링에서 거의 구분되지 않음(더미 데이터 렌더링+확대 스크린샷으로 확인, P3 — 데이터 판독엔 영향 없음) | 배색 매핑표에 "그리드와 통일"로 명시된 의도된 절충이었으나 epoch 경계 표시라는 원래 목적이 무력화됨 | `_EPOCH`를 그리드보다 밝은 앱 표준 토큰 `#4b5563`("테두리 강조/비활성")로 분리 — 리더가 직접 1줄 수정, `py_compile` 확인 완료 |
| BUG-008 | 디자인 톤 재검토 라운드5 | 학습 탭 `_queue_splitter`(커밋 `a32a8b5`)와 추론 탭 `outer_splitter`/3분할 스플리터(커밋 `b09fb83`)가 `setMinimumHeight/Width()`를 지정해뒀음에도 핸들을 끝까지 드래그하면 해당 pane이 0px로 완전히 붕괴(제목까지 소실). 라벨링 탭 서브스플리터(`f086636`)도 동일 패턴이라 같은 위험 공유 | `QSplitter.setChildrenCollapsible(False)`를 호출하지 않아 Qt 기본값(`childrenCollapsible=True`)이 적용됨 — `setMinimumHeight/Width`는 창 리사이즈 제약일 뿐 수동 드래그 collapse는 막지 못함(Qt 기본 동작) | 리더가 직접 `setChildrenCollapsible(False)`를 5개 스플리터 전부(학습 `_queue_splitter`, 추론 `outer_splitter`+3분할 `splitter`, 라벨링 `_splitter`+`_left_splitter`+`_right_splitter`)에 추가, `py_compile` 확인 완료(커밋 `7a98760`). **재검증 완료(2026-08-20, verifier)** — `python main.py` 실제 GUI 드래그로 5개 스플리터 전부 양방향 극단 이동 확인: 학습 탭 `_queue_splitter`(큐박스 min 120px / 모니터패널 min 150px), 추론 탭 `outer_splitter`(상단 체크포인트영역 min 140px / 메인영역 min 200px)와 3분할 `splitter`(목록패널 min 140px / 범례패널 min 160px), 라벨링 탭 `_splitter`(좌패널 min 150px / 우패널 min 130px)·`_left_splitter`(이미지브라우저 min 80px / 클래스패널 min 80px)·`_right_splitter`(어노테이션목록 min 80px / 로그패널 min 80px) 전부 설정된 최소 크기에서 정확히 멈추고 0px 붕괴·제목 소실 없음. 각 탭에서 정상 범위 중간 드래그(자유 리사이즈)도 문제없이 동작 확인 — `setChildrenCollapsible(False)`가 일반 리사이즈를 막지 않음. |
| BUG-011 | GitHub #3(b) 검증 후속 | 브러시 계열 도구 활성 상태에서 캔버스 더블클릭으로 브러시 크기 다이얼로그(GitHub #3(b), 커밋 `29248fa`)를 열면, 더블클릭의 첫 클릭(press+release)이 `mouseDoubleClickEvent()`보다 먼저 처리되어 클릭 지점에 원치 않는 `[Mask]` 어노테이션이 추가됨(OK/Cancel 무관하게 남음, Undo로만 제거 가능) | `mouseDoubleClickEvent()`가 브러시 페인트 자체를 막는 로직이 없어 press→release 사이 커밋된 미세 스트로크가 그대로 남음 | 브러시/버킷/지우개/영역지우개 분기 진입 시 다이얼로그를 띄우기 전에 `self.undo()` 호출 — `mousePressEvent`가 브러시 페인트 시작 시 항상 `_push_undo()`를 먼저 호출하므로, undo 1회가 정확히 이번 클릭 시퀀스의 stray 스트로크만 되돌림(그 이전 정상 작업엔 영향 없음). 리더가 직접 1줄 수정, `py_compile` 확인 완료. **재검증 완료(2026-08-20, verifier)** — `QTest.mouseDClick()`으로 실제 Qt 더블클릭 이벤트 시퀀스를 재현(Cancel 1회 + OK 3회 반복), 매번 stray 어노테이션 0건·브러시 크기는 정상 반영됨을 확인. 회귀 확인: 일반 연속 페인팅은 여전히 정상 커밋, Ctrl+Z(undo) 단축키도 정상 동작. |
| BUG-015 | 디자인 7단계 ⑦ 검증 후속 | `training_tab.py` CUDA 배너(커밋 `8f78f8a`)의 좌측 색상 보더가 단일 선이 아니라 ~7px 간격을 두고 두 개의 막대("[[" 형태)로 이중 렌더링됨(성공/실패 두 상태 모두) | `banner.setStyleSheet("background:...; border-left:...")`처럼 선택자 없이 평문 속성만 나열하면 Qt가 `*{...}`와 동일하게 서브트리 전체에 전파 — 자식 `QLabel`이 `QFrame` 서브클래스라 `WA_StyledBackground` 없이도 스스로 같은 `border-left`를 그려, 부모 보더(x≈0)와 자식 보더(레이아웃 좌측마진만큼 밀린 x≈10)가 별개 막대로 겹쳐 보임 | `banner.setObjectName("cudaBanner")` 후 스타일시트를 `"QWidget#cudaBanner { ... }"` ID 선택자로 범위를 좁혀 자손 전파 차단(검증 에이전트가 제시한 해결책 (a) 채택) — 3곳(`_build_cuda_banner()` 초기 스타일 + `_on_cuda_diag_done()`의 성공/실패 두 분기) 전부 동일 패턴으로 수정, 리더가 직접 처리, `py_compile` 확인 완료. **재검증 완료(2026-08-21, verifier)** — 실제 `TrainingTab` 인스턴스를 띄워 `_on_cuda_diag_done()`을 성공/실패 두 상태로 직접 호출 후 `QWidget.grab()` 픽셀 스캔. 배너 상단/하단 부근(텍스트 baseline과 겹치지 않는 y=1,3,5,28,30)에서 x=0~2에만 단일 3px 보더(성공 `#10b981`/실패 `#f87171`)가 나타나고 x=3~19 전 구간이 배경색 `#1f2329`로 균일함을 확인 — 이전에 보였던 x≈10~12 두 번째 막대 완전히 사라짐. 배경/모서리 둥글기(`border-radius:4px`), 라벨 텍스트·"진단 보기" 버튼 geometry도 밀림·잘림 없이 정상. 실패(빨강) 상태는 실제 GPU 미가용 환경 없이 결과 객체를 직접 주입해 동일하게 확인(코드가 성공 분기와 완전 대칭 구조). |
| BUG-012 | GitHub #3/#4/#6-A/#7 라운드2 검증 후속 | 라벨링 탭에서 GitHub #7 "예" 경로(라벨 삭제 후 OK) 직후엔 사이드바 아이콘이 정확하지만, 다른 이미지 갔다 돌아오면 "미라벨"로 되돌아감(디스크 JSON·툴바 체크는 항상 정확, 표시 캐시만 stale) | `_on_toggle_ok()`의 flush 단계가 호출하는 옛 `_do_save()`(daemon 스레드로 비동기 `store.save()` 후 즉시 `annotation_saved` emit)와, 바로 뒤이은 `toggle_ok()`의 동기 `get_ok()`/`set_ok()` 읽기-수정-쓰기가 **같은 파일에 대한 순서 보장 없는 두 writer**로 경쟁 — 나중에 끝나는 쪽이 이기는 레이스라 `image_browser.refresh_item()`(디스크 재조회 자체는 정상 동작)이 레이스에서 앞선 emit 시점의 상태를 캐싱해버림 | `_do_save()`에 `sync: bool` 매개변수 추가 — `True`면 스레드 없이 동기로 즉시 저장. `_on_toggle_ok()`의 flush를 `_do_save(sync=True)`로 호출해 `toggle_ok()`가 같은 파일을 건드리기 전 쓰기 완료를 보장. 다른 `_do_save()` 호출부(500ms 디바운스, 이미지 전환 전 flush)는 경쟁하는 두 번째 writer가 없어 그대로 둠. 커밋 `5d551c3`. **재검증 완료(2026-08-20, verifier)** — 실제 어노테이션이 있는 이미지(`11번`)로 OK 확인창 "예" 흐름을 거친 뒤 다른 이미지로 갔다 돌아오기를 6회 반복, 매 라운드 사이드바 캐시=`ok`·디스크 JSON `ok:true`·`annotations:[]` 완전 일치(불일치 0건). 회귀 확인: 무라벨 이미지 OK 토글(확인창 없는 경로) on/off 정상, 이미지 전환 시 디바운스 저장 flush(미저장 브러시 스트로크 자동 저장)도 정상 동작. |
| BUG-013 | GitHub #3/#4/#6-A/#7 라운드2 검증 후속 | 브러시 크기 더블클릭 다이얼로그 문자열이 `t()` 체계 밖 한국어 하드코딩 — English 설정에서도 이 다이얼로그만 한국어로 표시됨 | 이번 라운드(GitHub #3(b))에서 신규 추가된 문자열이라 기존 i18n en 정비(193키) 범위 밖에 있었음 | `i18n.py`에 `tool.brush_size_dialog.title`/`.label` 키를 ko/en 양쪽에 추가, `annotation_canvas.py`에 `t` import 후 하드코딩 문자열을 `t()` 호출로 교체. 커밋 `5d551c3`. **재검증 완료(2026-08-20, verifier)** — `i18n.set_language("en")`에서 `t("tool.brush_size_dialog.title")`="Brush Size", `.label`="Brush size (1~200)" 확인. `ko`로 되돌리면 "브러시 크기"/"브러시 크기 (1~200)"로 정상 복귀. |
| BUG-017 | R(추론 threshold/Excel) 검증 중 발견 | 기존 이미지가 있는 프로젝트를 열면 라벨링 탭 진입 시 이미지 목록 첫 항목이 선택 표시(하이라이트)되지만 캔버스는 빈 상태로 남아 사용자가 직접 클릭해야만 로드됨 | `ImageBrowser.__init__()`이 `reload()`(→ `_apply_display()`)로 첫 이미지를 자동 선택하며 `image_selected`를 즉시 발행하는데, 이 시점은 `LabelingTab._build_ui()` 내부(위젯 생성 중)라 `LabelingTab._connect_signals()`로 리스너를 연결하기 **전**이라 신호가 유실됨 — MainWindow 초기화 순서상 프로젝트는 이미 열려 있어(project_start_dialog에서 `set_current()` 먼저 호출) 실사용 흐름에서 항상 재현됨(라벨링 탭이 기본 활성 탭) | `LabelingTab.__init__()`에서 `_connect_signals()` 직후 `self._image_browser.current_path()`가 있으면 `self._on_image_selected()`를 명시적으로 1회 호출해 연결 완료 후 현재 선택을 캔버스에 동기화. 검증 에이전트가 직접 수정. **재검증 완료(2026-08-25, verifier)** — 격리 테스트 프로젝트(이미지 3장)로 `LabelingTab` 신규 생성 직후 `canvas._image_path`가 첫 이미지로 자동 설정됨을 확인(수정 전엔 `None`으로 재현됨). |
| BUG-029 | 미출시(2026-08-29 setup.iss 1줄 수정, 리더) | GitHub #22 구현이 Inno Setup 컴파일 자체를 깨뜨림 — `[Code]` 섹션 Pascal Script 안에서 `Format(...)` 호출의 인수 배열 `[OldVersion]`이 줄 맨 앞에 오도록 개행되어 있었음 | Inno Setup 컴파일러는 `[Code]` 섹션 내부에서도 줄 시작 문자가 `[`이면 무조건 새 INI 섹션 헤더로 오인해 파싱함(문서화되지 않은 알려진 함정) | 리더가 84~85행 `'계속하시겠습니까?', [OldVersion])`을 한 줄로 합쳐 `[`로 시작하는 줄이 없도록 수정. **재검증 완료(2026-08-29, verifier)** — `git diff HEAD -- installer/setup.iss`로 수정 확인 후 `build.bat` 전체를 실제로 끝까지 실행(PyInstaller onedir + `ISCC.exe installer\setup.iss`)해 컴파일이 `Invalid section tag` 에러 없이 성공, `installer\output\SegmentationModelUI-Setup-1.10.5.exe`(약 2.15GB)가 새로 생성됨을 파일 타임스탬프로 확인. `Successful compile (592.765 sec)` 로그로 컴파일 성공 재확인 |
| BUG-016 | 미출시(GitHub #22 구현에 포함, `[UninstallDelete]`) | Inno Setup 무인 제거 후 `data\logs\` 하위 런타임 로그 파일이 남음 | 로그 파일이 `[Files]`로 설치된 적이 없어 Inno Setup 기본 제거 로직이 추적하지 못함 | `[UninstallDelete] Type: filesandordirs; Name: "{app}\data\logs"` 추가(images/annotations/checkpoints 등 사용자 데이터가 있는 `data\` 전체가 아니라 `logs\`만 명시적으로 제거 대상에 포함). **재검증 완료(2026-08-29, verifier, 실제 빌드 exe로 실기기 재현)** — ① 신규 설치 후 앱을 실제로 30초 이상 구동해 `data\logs\app.log`/`errors.log`가 실제로 생성됨을 확인(Start Menu/바탕화면 바로가기와 동일하게 `WorkingDir={app}`로 실행할 때만 정상 위치에 생성됨 — 아래 비고 참고). ② `unins000.exe /VERYSILENT /SUPPRESSMSGBOXES` 무인 제거 후 `data\logs\`와 그 안의 로그 파일 완전 삭제 확인. ③ 별도 설치(install2)에 `data\images\sample.png`, `data\annotations\sample.json`, `data\checkpoints\epoch_0001.pt`, `data\logs\*.log` 더미 파일을 만든 뒤 동일하게 무인 제거 → `data\logs\`만 삭제되고 images/annotations/checkpoints 3개 파일은 모두 그대로 보존됨을 직접 확인(요구사항 그대로 충족). **비고(별개의 경미한 관찰, 신규 버그 등록은 안 함)**: `{app}`와 빈 `data\` 폴더 자체는 삭제 후에도 디스크에 남음(Inno Setup 표준 동작 — `{app}`은 완전히 비어 있어야만 자동 삭제되는데 `[UninstallDelete]`가 `data\logs`만 명시적으로 지정했으므로 그 상위 폴더들은 대상 밖). 원래 BUG-016이 지목한 핵심 증상(로그 "파일" 잔존)은 완전히 해소됨 — 빈 폴더 잔존은 사용자 데이터 손실이나 실질적 디스크 오염이 아니므로 이번 라운드에서는 별도 티켓화하지 않음. 또한 `app/core/logger.py`의 `LOG_DIR = Path("data/logs")`가 실행 파일 위치가 아닌 **프로세스 현재 작업 디렉터리(cwd) 기준 상대경로**라서, Start Menu/바탕화면 바로가기(둘 다 `WorkingDir={app}` 지정됨)나 탐색기 더블클릭이 아닌 다른 방식(예: cwd가 다른 상태에서 `start` 명령으로 실행)으로 앱을 띄우면 로그 파일이 `{app}\data\logs\`가 아닌 엉뚱한 위치에 생길 수 있음을 실측으로 확인(`main.py`의 `ensure_data_dirs()`는 `sys.executable` 기준 절대경로를 써서 이 문제가 없음 — 두 모듈의 경로 해석 방식이 다름). 정상 설치 후 제공되는 유일한 두 실행 진입점(Start Menu·바탕화면 아이콘)은 영향받지 않으므로 이번 BUG-016 판정에는 포함하지 않되, 리더/구현자가 참고할 수 있도록 기록만 남김 |
| BUG-030 | 1.10.5(installer, 2026-08-30 커밋 예정) | GitHub #22 핵심 기능(기존 버전 감지 후 자동 제거)이 **항상 무조건 실패**함 — 실제 기존 버전이 설치되어 레지스트리에 정상 존재해도 절대 감지되지 않아 안내 MsgBox가 결코 뜨지 않고, 매번 기존 버전이 없는 것처럼 곧바로 새 설치가 진행됨(구버전 자동 제거도 당연히 실행 안 됨) | `GetUninstallRegKey()`의 `SetupSetting("AppId")` — `[Setup]` 섹션의 `AppId={{{#MyAppId}}` 표현식에서 Inno가 `"{{" -> "{"`로 이스케이프 해제하기 **이전의** 원시 텍스트(`{{GUID}`, 중괄호 2개)를 반환해, 실제 언인스톨 레지스트리 키(`{GUID}_is1`, 중괄호 1개)와 영원히 불일치했음(BUG-029 컴파일 수정 후 첫 실행 확인에서 발견) | `SetupSetting("AppId")` 대신 `{#MyAppId}` 전처리기 매크로(중괄호 없는 순수 GUID 텍스트)를 직접 참조해 `Result := 'Software\Microsoft\Windows\CurrentVersion\Uninstall\{{#MyAppId}}_is1';`처럼 중괄호 1쌍만 직접 감싸도록 수정. `tests/test_build_release.py`에 `test_uninstall_reg_key_matches_actual_windows_uninstall_key_format` 신설 — ISPP 매크로 치환을 그대로 재현해 최종 레지스트리 키 문자열까지 실제 계산해 검증(기존 두 테스트는 문자열 존재 여부만 확인해 이 회귀를 못 잡았음). **최종 독립 재검증 완료(2026-08-30, verifier, 구현자 스크립트 재사용 없이 처음부터 전부 재현)**: (1) `MSYS_NO_PATHCONV=1 cmd /c build.bat` 전체 파이프라인(PyInstaller onedir + `ISCC.exe`)을 실제로 처음부터 끝까지 재실행 — 591.8초 만에 컴파일 성공, BUG-029(Pascal `[` 파싱 오류) 재발 없음, 새 exe(2,147,000,000바이트대, 타임스탬프 00:24 새로 생성) 확인. (2) 구버전 시나리오를 이번엔 **자기 자신 재실행이 아니라 실제 이전 릴리스 `installer/output/SegmentationModelUI-Setup-1.10.1.exe`를 무인 설치**해 기존 버전을 준비(더 강한 재현 — 서로 다른 빌드 간 업그레이드 시나리오), `reg query`로 `HKCU\...\Uninstall\{03C2678A-...}_is1` 키에 `DisplayVersion=1.10.1`이 실존함을 확인. (3) 새 1.10.5 설치 프로그램을 `/SILENT`(＝`/SUPPRESSMSGBOXES` **미지정**, 대화형 메시지박스 노출)로 실행해 실제 Win32 `#32770` 대화상자가 뜸을 `EnumWindows`/`GetClassName`으로 직접 확인 — 자식 컨트롤 텍스트가 `기존 버전 1.10.1이(가) 설치되어 있습니다. ... 계속하시겠습니까?`로 실제 감지된 구버전 번호(1.10.1)가 정확히 반영됨을 확인, `예(&Y)` 버튼에 `BM_CLICK` 메시지를 보내 실제 클릭 → 이후 95초 안에 프로세스가 스스로 종료(행 없음), 레지스트리 `DisplayVersion`이 1.10.5로 갱신되고 `unins000.exe` → `unins001.exe`로 번호가 실제로 바뀜을 확인(구현자 관찰과 별도 재현으로 일치). (4) 업그레이드 도중 구버전 설치 폴더에 미리 심어둔 더미 `data\images`/`data\annotations`/`data\checkpoints` 파일이 업그레이드 후에도 3개 전부 그대로 보존됨을 확인. (5) 업그레이드된 1.10.5 앱을 실제로 30초 가까이 구동해 `data\logs\app.log`가 실제로 생성됨을 확인한 뒤 `unins001.exe /VERYSILENT /SUPPRESSMSGBOXES`로 무인 제거 → 레지스트리 키 완전 삭제, `data\logs\` 완전 삭제, 반면 앞서 심은 images/annotations/checkpoints 더미 파일 3개는 모두 그대로 보존됨을 재확인(BUG-016 결합 재검증). (6) 회귀 확인 — 구버전이 전혀 없는 상태에서 1.10.5를 `/VERYSILENT /SUPPRESSMSGBOXES`로 새로 설치 시 팝업 없이 105.7초 만에 정상 완료(감지 로직이 과잉 동작하지 않음 확인). (7) `tests/test_build_release.py` 전체를 `C:\Users\Feel\anaconda3\python.exe`로 재실행 — 신설 테스트 포함 10건 통과(`tmp_path` 픽스처를 쓰는 나머지 테스트는 이 환경의 기존 `pytest-of-Feel` 폴더 권한 문제로 실패, 이번 변경과 무관한 기존 환경 이슈 재확인) — 테스트 코드 리딩으로 실제 계산된 레지스트리 키 값까지 비교하는 유의미한 회귀 테스트임을 확인(문자열 존재 여부만 보는 이전 테스트들과 다름). 테스트 후 설치 폴더·레지스트리 키·`dist\SegmentationModelUI\data\` 등 테스트로 생성된 모든 흔적 정리 완료, 실제 프로젝트 `data\`는 건드리지 않음 |
| BUG-031 | 1.10.5(installer, 2026-08-30 커밋 예정) | GitHub #22 무인 자동 업그레이드 기능이 BUG-030 수정 직후 새로 드러난 별개의 근본 원인으로 인해 **무인(`/VERYSILENT /SUPPRESSMSGBOXES`) 재설치 시 영원히 멈춤(hang)** — 레지스트리 키가 일치해 기존 버전 감지 자체는 성공하지만, 안내/실패 확인용으로 쓰던 `InitializeSetup()`의 일반 `MsgBox()` 2곳이 실제 Windows 대화상자(`#32770`)를 띄우며 사용자 입력을 무한 대기함 | Inno Setup의 일반 `MsgBox()`는 `/SUPPRESSMSGBOXES` 플래그를 무시하고 항상 진짜 모달을 띄움 — 이 플래그를 실제로 존중하는 함수는 `SuppressibleMsgBox()`뿐임(BUG-030에 가려 이전 라운드까지는 감지 로직 자체가 발동하지 않아 드러나지 않았던 문제, 구현자가 BUG-030 수정 직후 실제 재현 테스트 중 직접 발견) | `InitializeSetup()`의 확인 `MsgBox`(기본 응답 `IDYES` — 무인 모드에서는 자동 업그레이드 진행이 기본)와 실패 안내 `MsgBox`(기본 응답 `IDOK`) 2곳 모두 `SuppressibleMsgBox(...)` 로 교체. **독립 재검증 완료(2026-08-30, verifier)** — 위 BUG-030 항목의 (2)~(6) 전체 무인/대화형 사이클 재현 과정에서 함께 확인: 무인 모드(`/VERYSILENT /SUPPRESSMSGBOXES`)에서는 대화상자 없이 자동으로 `IDYES` 취급되어 곧장 진행되고 행 없이 종료(exit 0), 대화형 모드(`/SILENT`, `/SUPPRESSMSGBOXES` 미지정)에서는 실제 대화상자가 뜨고 `예` 클릭으로 정상 진행됨을 확인 — 두 모드 모두 이전처럼 멈추지 않음 |
| BUG-026 | 미출시(2026-08-30 커밋 예정) | `TrainerWorker`(커밋 `450f520` "학습 큐 중지 응답 개선") — 학습 **1 epoch째** 도중(첫 배치~validation 어느 시점이든)에 Stop을 누르면 체크포인트가 **단 1개도** 저장되지 않고 `training_metrics.json`/`.png`도 전혀 생성되지 않음(2번째 이후 epoch에서 멈추면 직전 epoch 결과는 정상 보존됨 — 경계는 정확히 "1 epoch도 완주 못 함"). `epoch_done`/체크포인트 저장/`history.append()`가 모두 그 epoch의 train+validation 루프를 완주해야 실행되는데, `450f520`이 추가한 mid-batch `if self._stop.is_set(): break`가 train 루프(라인 297~299)·validation 루프(라인 324~326) 양쪽에서 `history`가 채워지기 전에 epoch 루프 자체를 탈출시킴 — `if history:`(파일 저장 조건, 라인 394)까지 도달 못 해 통째로 스킵됨. 이전 동작(주석에 명시: "중지 요청 후에도 validation·metric·checkpoint까지 계속 실행됐다")에서는 최소 1 epoch는 항상 보존됐음 — 즉시 중지 응답성 개선이 "언제 멈춰도 최소 1개는 남는다"는 기존 보장을 깨뜨린 회귀. **재현 확인(2026-08-29, verifier)**: 합성 데이터셋(이미지 4장, 64×64) + `TinyModel`로 `TrainerWorker.run()`을 직접 호출, `batch_done` 첫 신호에서 `request_stop()` 호출 → 체크포인트 0개, `training_metrics.json`/`.png` 둘 다 미생성 확인. epoch 2에서 동일하게 중단하면 epoch 1 결과(체크포인트 2개, 두 파일)가 정상 보존됨도 함께 확인(경계 확정) | `450f520`이 train/validation 루프 양쪽에 mid-batch `if self._stop.is_set(): break`를 추가하면서 `history.append()`/체크포인트/`training_metrics.json`·`.png` 저장 지점(epoch 루프 본문 끝)까지 도달하기 전에 epoch 루프 자체를 탈출시켜버림 | `app/core/trainer.py` — train 배치 루프는 mid-batch stop 시 `stopped_early` 플래그만 세우고 남은 배치를 스킵(응답성 유지, `batches_processed`로 `train_loss` 정확히 평균)하되 epoch 루프 자체는 조기 탈출하지 않도록 변경. validation은 stop 여부와 무관하게 항상 완주하도록 mid-batch/종료 후 stop 체크 2곳 모두 제거 — 다음 epoch 진입 차단은 for 루프 최상단의 기존 체크 1곳으로 정리. `tests/test_training_performance.py` 3건(1 epoch 미만 중지 시 체크포인트/지표 저장, 중지 없는 2 epoch 정상 완주, 2번째 epoch 도중 중지) 신규. **독립 재검증 완료(2026-08-30, verifier)** — `pytest tests/` 90 passed 재실행 확인 + 실제 `TrainerWorker`(QThread) + 실제 디스크 이미지/어노테이션으로 골든 패스 재현: 1 epoch 미만 중지 시 체크포인트·`training_metrics.json`/`.png` 실제 생성 확인, 같은 환경에서 측정한 완주 베이스라인(5.51s) 대비 중지 케이스(2.14s)가 유의미하게 빠름(응답성 실측, 베이스라인의 75% 미만). 2 epoch 정상 완주·2번째 epoch 도중 중지 회귀 케이스도 실제 QThread로 재확인 |
| BUG-027 | 미출시(2026-08-30 커밋 예정) | `_InferenceWorker`(커밋 `ca30948` "이슈 23 일괄 추론 및 학습 결과 저장")가 `QThread.run()` 안에서 `engine.run()`/`engine.run_sliding_window()`를 직접 호출하는데, 이 함수들이 내부적으로 부르는 `_colorize_and_blend()`(`app/core/inference_engine.py`)가 `QImage`+`QPixmap.fromImage()`로 오버레이 픽스맵을 **GUI(메인) 스레드가 아닌 백그라운드 QThread에서** 생성함 — Qt 공식 문서상 QPixmap은 GUI 스레드 전용(QImage와 달리 스레드 세이프하지 않음)이며, CLAUDE.md의 "외부 상태 변경은 Qt 시그널·슬롯을 통해서만 수행(스레드 안전)" 원칙에도 위배. `ca30948` 이전에는 `_on_run()`이 `engine.run()`을 GUI 스레드에서 동기 호출했으나(UI가 블로킹됨), 일괄 추론 논블로킹화 과정에서 픽스맵 생성 지점까지 함께 백그라운드로 옮겨간 회귀. **재현 확인(2026-08-29, verifier)**: 실제 `InferenceTab` 위젯을 띄우고 합성 이미지 3장 + 체크포인트로 "▶ 추론 실행" 버튼 핸들러(`_on_run()`)를 실제 호출 → `engine.run()`이 호출된 3번 모두 메인 스레드가 아닌 워커 스레드에서 실행됨을 스레드 아이덴티티로 직접 확인. 이번 환경(Windows 11, Qt 6.7.1, raster 백엔드)에서는 크래시나 깨진 렌더링 없이 3개 결과 모두 정상 생성됐음(즉시 크래시는 재현 안 됨) — 다만 이는 Qt 문서가 명시한 "정의되지 않은 동작"이 이 특정 백엔드/환경에서 우연히 동작한 것일 뿐, 다른 Qt 백엔드(OpenGL/ANGLE 렌더링, 원격 데스크톱, 고DPI 스케일링 등)에서는 잠재적으로 크래시·손상 렌더링 위험이 있음. 근본 수정 방향: 워커 스레드에서는 `QImage`만 만들어 시그널로 넘기고 `QPixmap.fromImage()` 변환은 수신 슬롯(GUI 스레드)에서 수행 | `_colorize_and_blend()`가 `QPixmap.fromImage()`로 오버레이를 만들어 반환했는데, 이 함수를 호출하는 `run()`/`run_sliding_window()`/`refilter()`가 `_InferenceWorker.run()`(백그라운드 QThread)에서 실행됨 | `app/core/inference_engine.py` — `_colorize_and_blend()`/`reblend()` 반환 타입을 `QPixmap`→`QImage`로 변경, `InferenceResult.overlay_pixmap: QPixmap` → `overlay_image: QImage`로 필드명·타입 변경. 모듈에서 `QPixmap` import 자체를 제거해 구조적으로 재발 방지. `app/tabs/inference_tab.py`의 `_show_current_image()`(GUI 스레드에서만 실행되는 슬롯)로 `QPixmap.fromImage()` 변환 지점을 이동. `tests/test_github_issue_23.py`에 `test_inference_engine_never_touches_qpixmap`(구조적 가드)·`test_bug_027_worker_runs_off_gui_thread_and_returns_qimage`(실제 QThread로 스레드 아이덴티티 확인) 신규. **독립 재검증 완료(2026-08-30, verifier)** — `pytest tests/` 90 passed 재실행 확인 + 실제 `InferenceTab` 위젯 + 실제 `_InferenceWorker` QThread로 골든 패스 재현: 체크포인트 선택→배치 추론→오버레이 표시→불투명도/threshold 슬라이더 조작→Excel 내보내기 전부 크래시 없이 통과. `engine.run`/`_colorize_and_blend`를 스파이로 감싸 `threading.get_ident()`로 실측 — 실제 GUI 스레드와 다른 스레드에서 실행됨을 직접 확인, `QPixmap.fromImage()`는 `_show_current_image()`(GUI 스레드) 안에서만 호출됨을 코드 경로로 재확인. `overlay_pixmap` 잔존 참조 전수 grep — main(`app/`) 범위엔 없음 |

---

## VOC (Voice of Customer)

사용자가 직접 제기한 요청·불편 사항.

| 날짜 | 요청 내용 | 결정 | 근거 |
|------|-----------|------|------|
| 2026-08-20 | [GitHub #1](https://github.com/sfeelBot/Segmentation-Tool/issues/1) "사용성 정리" ① 라벨링/추론 이미지 탭에 폴더 정보(최하위 폴더명) 표시 요청 | 부분 수용 — 추론 탭은 라운드 2에서 트리형 폴더 그룹핑 신규 추가(실제 재귀 스캔, `inference_image_list.py`). **최종 정정(2026-08-21, BUG-005 처리 완료)**: "라벨링 탭은 기존부터 폴더 트리 있음"이라는 이전 응답은 오답이었음 — 라벨링 탭은 폴더 그룹핑을 지원하지 않음(단일 평탄 목록). 실제로 지원하려면 `app/core/dataset.py` 학습 스캔 로직까지 함께 바꿔야 하는 별도 기능 개발이라 이번엔 도달 불가능했던 죽은 코드만 제거함 — 필요 시 별도 기능 요청으로 재논의 | `docs/roadmap.md` "UI/UX 재편" 절, QA.md BUG-005(Closed) |
| 2026-08-20 | [GitHub #1](https://github.com/sfeelBot/Segmentation-Tool/issues/1) "사용성 정리" ② RTX 4500 Ada GPU인데 `torch 2.13.0+cpu`(CPU 전용)로 설치됨, torchvision 설치 실패 원인 문의, .exe 빌드 시 의존성 별도 설치 필요 여부 문의 | 원인 파악 완료 — `requirements.txt`에 CUDA index-url 안내가 없어 `pip install -r requirements.txt`를 먼저 실행하면 CPU 전용 빌드가 잡힘. `docs/USER_MANUAL.md`엔 이미 올바른 설치 순서(torch를 CUDA 빌드로 먼저 설치)가 있었으나 루트 `README.md`가 빈 플레이스홀더라 발견이 안 됨 | **즉시 수정**: `README.md`에 설치 가이드 추가(USER_MANUAL 링크 포함), `requirements.txt`에 경고 주석 추가, `USER_MANUAL.md` GPU 표에 RTX Ada 워크스테이션 계열 행 추가. .exe 빌드 의존성 별도 설치 문제는 CLAUDE.md상 PyInstaller 패키징이 범위 밖이라 별도 논의 필요 |
| 2026-08-20 | [GitHub #2](https://github.com/sfeelBot/Segmentation-Tool/issues/2) "프로젝트 내보내기 기능" ① 프로젝트를 클릭하면 자동으로 열리게 해달라 | **사용자 재확인으로 정정**: 앱 내부 최근 프로젝트 목록 얘기가 아니라 "전용 프로젝트 확장자를 만들어 OS 파일탐색기에서 더블클릭하면 앱이 바로 열리게" 해달라는 요청이었음. Windows 파일 연결은 보통 설치 프로그램이 등록하므로 exe 패키징(별도 "추후" 항목)에 하위 요구사항으로 편입 — 지금 스코프 아님 | `docs/roadmap.md` "exe 패키징 + Setup Guide" 절 |
| 2026-08-20 | [GitHub #2](https://github.com/sfeelBot/Segmentation-Tool/issues/2) "프로젝트 내보내기 기능" ② 프로젝트 전체를 내보내거나 하나의 파일로 통합하는 기능 요청 | 수용 — export(라운드 A: zip 패키징) + import(라운드 B: zip→프로젝트 복원) 둘 다 진행 확정. checkpoints 기본 미체크/user_models 기본 체크, import 이름 충돌 시 자동 리네임(덮어쓰기 금지) | `docs/specs/voc-github-issues-2026-08-20.md` |

---

## 이슈 작성 가이드

### Bug ID 형식
`BUG-{순번}` (예: `BUG-001`)

### 우선순위
- **P0** — 앱 크래시·데이터 손실
- **P1** — 핵심 기능 동작 불가
- **P2** — 기능 동작하나 불편함
- **P3** — 개선 사항·UX 요청

### Bug 항목 예시
```
| BUG-001 | P1 | 폴리곤 닫기 후 Undo가 동작하지 않음 | 사용자 VOC | 조사 중 |
```

### VOC 항목 예시
```
| 2025-01-15 | 브러시 크기 단축키 추가 요청 | 수용 — Phase 3에서 구현 | 사용성 향상 |
```

## 2026-08-28 — GitHub #23 학습·추론 개선

- 추론 탭: 전체 이미지 비동기 일괄 처리, 이미지별 진행률, 추론 전 원본 표시, `F` 오버레이 전환.
- 학습 탭: 최고 IoU 체크포인트(`*_best.pt`)와 모델별 지표 JSON/그래프 PNG 자동 저장.
- 검증: `test_github_issue_23.py`, 학습 성능, sliding-window 관련 10개 테스트 통과.
- 전체 pytest 수집은 기존 Windows QtSvg DLL 로드 순서 문제로 중단되어 관련 묶음으로 검증함.

## 2026-08-28 — 일반 셸 최초 빌드 의존성 자동 설치

- 재현: 새 시스템 Python 3.12에서 `build.bat` 실행 시 `ModuleNotFoundError: PyQt6`로 종료.
- 조치: `build\venv` 자동 생성, CUDA PyTorch와 requirements/PyInstaller 최초 1회 자동 설치.
- 회귀 기준: 사용자가 conda/venv를 활성화하지 않아도 모든 빌드 단계가 전용 Python을 사용해야 한다.
