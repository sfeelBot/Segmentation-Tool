PyQt6==6.7.1       # 6.8+ 은 Windows Anaconda 환경에서 DLL 로드 실패

# torch/torchvision: 이 파일로 바로 설치하면 CPU 전용 빌드가 깔립니다.
# 먼저 GPU에 맞는 CUDA 빌드를 설치하세요 (docs/USER_MANUAL.md 참고):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.3.0
torchvision>=0.18.0
Pillow>=10.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
albumentations>=1.4.0
openpyxl>=3.1.0
matplotlib>=3.8.0
