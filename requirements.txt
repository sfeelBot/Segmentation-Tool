PyQt6==6.7.1       # 6.8+ fails to load DLLs on Windows Anaconda environments

# torch/torchvision: installing straight from this file pulls a CPU-only build.
# Install the CUDA build matching your GPU first (see docs/USER_MANUAL.md):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Keep this tested pair pinned: newer Python 3.12 cu128 wheels have caused
# c10.dll WinError 1114 during clean installer builds.
torch==2.7.1
torchvision==0.22.1
Pillow>=10.3.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
albumentations>=1.4.0
openpyxl>=3.1.0
matplotlib>=3.8.0
