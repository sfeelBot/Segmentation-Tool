# 디자인 (Design) 로그

역할 설명은 [README.md](README.md) 참고. append-only — 최신 항목이 아래에 추가된다.

---

## 2026-08-19 — 워크플로우 세팅, 검토 대기

이번 세션은 상태 파악 + 워크플로우 구축이 목적으로, UI/UX 변경 요청은 없었음.
다음 UI 관련 작업 요청 시 색상·레이아웃·단축키 일관성(특히 `main.py`의 다크 테마
팔레트: `#374151` 테두리, `#111418` 배경 등)을 여기에 기록한다.

---

## 2026-08-20 — 라운드 2(레이아웃 재구조화) 목업 완료, 사용자 확인 대기

`docs/specs/ui-redesign-plan-2026-08-19.md` 라운드 2(2026-08-20 결정 4건 반영본) 대상 시각
목업 작성. 코드는 건드리지 않음 — `git status` 확인 결과 `docs/agents/design-log.md` 외 변경
없음(기존 modified 상태인 `dataset.py`/`trainer.py`/`config_form.py`/`leader-log.md`는 이번
작업과 무관, 손대지 않음).

**Artifact URL**: https://claude.ai/code/artifact/5df2af11-0642-4e69-8ddc-f545333eebca

### 무엇을 목업했나 (Before/After 비교, 3탭 + 확대 비교 1개)
- **라벨링 탭**: 좌 패널(이미지브라우저↔클래스패널) · 우 패널(어노테이션목록↔로그패널)에
  세로 QSplitter 추가 — 초기 비율 3:2 / 2:3 유지, 드래그 그립(파란 hover 바)으로 표현.
  `ClassPanel` 200px 상한 제거 후 클래스 8개가 스크롤 없이 보이는 상태를 After에 반영.
- **학습 탭**: 큐 박스(작업추가+큐리스트+제어버튼) ↔ 진행상태/그래프/메트릭 영역 사이
  세로 QSplitter 추가. `_queue_list` maxHeight(140) 제거 후 큐 4개가 스크롤 없이 보이는
  상태를 After에 반영. 좌측 설정폼·그래프|사이드패널 가로분할은 변경 없음을 명시.
- **추론 탭**: 상단(파일컨트롤+추론방식+체크포인트테이블) ↔ 메인뷰어(목록|뷰어|범례) 영역
  사이 세로 QSplitter 추가, 테이블 maxHeight(120) 제거.
- **추론 탭 이미지 목록 신규 컴포넌트**: 64px 썸네일 아이콘 그리드 → 검색창+정렬콤보(파일명↑/
  파일명↓/폴더)+폴더 트리(라벨링 탭 `ImageBrowser`와 동일한 폴더헤더 `#60a5fa`/`#1a2235`,
  상태기호 스타일) 구조로 교체. 별도 "확대 비교" 섹션에서 64px 그리드 vs 트리 행을 실제 크기
  감각으로 나란히 배치해 트레이드오프(큰 프리뷰 상실)를 명확히 시각화.

### 색상·레이아웃 일관성 확인
`main.py` 전역 스타일시트 값을 그대로 사용 — 배경 `#1a1d23`(기본)/`#1f2329`(그룹박스·툴바)/
`#111418`(입력창·리스트), 테두리 `#374151`(기본)/`#4b5563`(강조), 강조색 `#60a5fa`, 성공
`#10b981`/`#34d399`, 경고 `#fbbf24`, 오류 `#f87171`. `QSplitter::handle:hover` 파란 강조는
기존 라벨링/학습/추론 메인 스플리터와 동일 패턴(회색→호버시 `#60a5fa`)으로 신규 서브
스플리터에도 통일 적용. `image_browser.py`의 트리 스타일(폴더헤더 `#1a2235`/`#60a5fa`,
상태기호 `●`/`✓`/`○` 색상 `#6ddf6d`/`#5ba8ff`/`#999999`)을 추론 탭 신규 컴포넌트 목업에
그대로 재사용해 시각적 통일성 확보. 단축키는 이번 라운드가 레이아웃 구조 변경만 다루므로
영향 없음(변경 없음 확인).

---

## 2026-08-20 — 아이콘 미니멀 리디자인 + i18n 계획 목업 완료, 사용자 확인 대기

`docs/roadmap.md` "아이콘/이모지 → 미니멀 디자인 + i18n(en) 완비" 2단계 대상. 코드는 건드리지
않음 — `git status` 확인 결과 기존 modified(`dataset.py`/`trainer.py`/`config_form.py`/
`leader-log.md`)만 있고 이번 작업으로 추가된 코드 변경 없음(이 파일만 append).

**Artifact URL**: https://claude.ai/code/artifact/f76ffe60-df35-4fad-b87b-f845f535dc29

### 구현 방식 추천 — (a) SVG 아이콘 + QIcon
1인/소규모 유지보수 데스크탑 툴이라는 전제하에 3안을 비교:
- **(a) SVG + QIcon [추천]**: MIT/ISC 라이선스 아이콘 팩(Feather/Lucide류)에서 15~18개를 골라
  `app/resources/icons/*.svg`로 보관, `QIcon(path)` 또는 `QSvgRenderer`로 로드. `currentColor`
  자리만 accent색으로 치환하면 hover/checked 상태도 같은 파일로 커버. 초기 에셋 파이프라인
  구축비용은 있지만 아이콘 개수가 적어 일회성이고, OS/폰트에 무관하게 렌더링이 일관됨 —
  "이모지 컬러폰트 렌더링 불안정"이라는 문제의 근본 원인을 실제로 없앤다.
- **(b) QPainter 직접 드로잉**: 외부 의존성 0이 유일한 장점. 15개 도형(브러시 털, 버킷
  손잡이 등)을 좌표로 손코딩해야 해 (a)보다 초기비용이 크고 결과물 품질도 낮기 쉬움 —
  이 프로젝트 규모엔 과설계로 판단, 비추천.
- **(c) 유니코드 기호 교체**: 비용 최저지만 근본 원인(이모지 폰트 렌더링)을 해결 못함.
  대신 **2단계 "장식 이모지 제거"에는 적합** — 텍스트로 바꾸거나 이미 모노톤인 기호
  (`·`/`•`/`▲`/`✕`)는 유지.
- 목업의 "After" 아이콘은 실제로 (a) 방식대로 그린 인라인 SVG(선굵기 1.6px 통일, 폴리곤/
  브러시/버킷/지우개/영역지우개/선택/팬 7종 + 참고용 부가액션)라 구현 시 기대 형태에 가까움.

### 4그룹 재설계 요지
1. **`labeling_tab.py` 툴바 7종**: 이모지 단독(16px) → SVG 라인 아이콘, 활성 도구만
   accent(`#60a5fa`) 배경/테두리. 텍스트 라벨은 안 붙임(7개 다 붙이면 툴바 과밀) — 대신
   형태 자체를 명확히 구분. 툴팁에 단축키 병기 유지.
2. **`image_browser.py` 상태기호 3종**: 색상만 다르던 것을 유지하되(모양은 이미 채움원/체크/
   빈원으로 구분됨) 크기·선굵기 확대 + 팔레트를 `main.py` 표준값(`#10b981`/`#60a5fa`)으로
   교체해 대비 개선. 범례 유지.
3. **`training_progress_dialog.py` STATUS_ICON 5종**: waiting(빈 원)/running(accent 채움
   원+테두리)/done(초록 원+체크)/error(빨강 원+X)/stopped(회색 사각) — 색상+모양 이중 구분.
4. **`log_panel.py` 로그레벨**: DEBUG(`·`)/INFO(`•`)는 이미 모노톤이라 유지, WARNING(`▲`)/
   ERROR(`✕`)도 유지(두께만 통일), CRITICAL의 `🚨`(컬러 이모지)만 같은 기하기호 계열 `■`로
   교체 — 이 항목만 이모지 폰트 의존을 완전히 제거.

### 장식 이모지 제거 예시 (2단계)
`project_start_dialog.py`("🎯 Segmentation Model UI"→"Segmentation Model UI"),
`auto_label_dialog.py`(창제목 "✨ 오토 라벨링"→"오토 라벨링", 그룹박스 "📊 현재 프로젝트..." 등
접두 이모지 전부 제거), `settings_dialog.py`(탭제목·버튼 접두 이모지 제거, **국기 이모지
🇰🇷/🇺🇸는 언어 식별에 실질적 도움이라 예외 유지**).

### i18n 전환 참고 (구현 시 참고, 코드 작성은 안 함)
`i18n.py`는 ko/en 193키 완비, 이모지가 번역 값 문자열 안에 있어 위 교체는 ko/en 양쪽에 자동
반영됨. 단 `image_browser.py`/`training_progress_dialog.py`/`main_window.py` 3개 파일은
i18n 밖에서 한국어를 직접 하드코딩(en 버전 자체 없음) — `t()` 호출로 이관 + `i18n.py`에 en 키
신설 필요. 라운드 1·2에서 최종 문구가 확정된 뒤 키를 뽑는 편이 재작업이 적음.

### 실행 순서 제안 (3라운드)
1. **4그룹 기능적 아이콘 SVG화** — `app/resources/icons/*.svg` 신설 + 로더 유틸. 가시성 저하
   민원의 핵심이라 최우선.
2. **장식 이모지 제거** — `auto_label_dialog.py`/`settings_dialog.py`/`model_presets/__init__.py`/
   `export_dialog.py` 등. `project_start_dialog.py`는 GitHub #2 라운드B(가져오기) 완료 후
   이 라운드에 포함(파일 겹침).
3. **i18n 밖 3파일 en 전환** — `image_browser.py`/`training_progress_dialog.py`/
   `main_window.py`를 `t()` 체계로 이관, `i18n.py`에 en 키 추가. 1·2에서 확정된 최종 문구
   기준으로 키를 뽑아야 재작업이 적어 마지막 순서.

### 색상·레이아웃 일관성 확인
`main.py` 팔레트(`#1a1d23`/`#1f2329`/`#111418`/`#374151`/`#4b5563`/`#60a5fa`/`#10b981`/
`#fbbf24`/`#f87171`/`#dc2626`)를 목업에 그대로 사용, 신규 색상 도입 없음. 단축키는 이번
작업 범위 밖(변경 없음).

### 구현 시 참고 — 주의사항
- **QTreeWidgetItem 구조 제안**: `image_browser.py`의 `_make_tree_item`/`_make_folder_item`
  패턴을 그대로 참고하되, 추론 탭은 라벨 상태 개념이 없으므로 상태기호(`●`/`✓`/`○`) 대신
  UserRole에 Path만 저장하고 텍스트는 파일명만 표시. 목업에서는 절충안으로 행 왼쪽에
  18~24px 미니 썸네일(`QIcon`/`QPixmap` 스케일)을 얹는 것을 제안했음 — 스펙 문서가 이미
  "가능한 절충"으로 언급한 내용이라 구현 재량으로 반영 여부 결정 가능(사용자 재확인 불필요).
- **세로 QSplitter 초기 비율**: `setSizes()`로 기존 stretch 비율(3:2, 2:3 등)과 동일한
  픽셀 비율을 지정할 것 — 목업은 이 "초기 상태는 기존과 동일, 이후 자유 조정"을 전제로
  그렸으므로 구현이 초기 비율을 다르게 잡으면 목업과 어긋난다.
- **`ClassPanel`/`_queue_list`/체크포인트테이블 상한 제거**는 상위 컨테이너가 QSplitter로
  바뀐 뒤에만 의미가 있다(사용자가 이미 확인한 전제) — 상한만 먼저 제거하고 스플리터 전환이
  누락되면 레이아웃이 무너질 수 있으니 두 변경을 같은 커밋/같은 검증 라운드에서 함께 처리할 것.
- 라운드 3(정보 추가) 대상은 이번 목업 범위 밖 — 신뢰도 컬럼, LR 표시, 줌/이미지크기 표시 등은
  별도 목업 필요.

**상태**: 목업 완료, 사용자 승인 대기 (구현 착수 전 필수).

---

## 2026-08-20 — 4탭 디자인 톤 홀리스틱 재검토 완료, 사용자 확인 대기

`docs/roadmap.md` "디자인 톤 홀리스틱 재검토" 대상. 아이콘 미니멀 리디자인 목업(승인됨,
Artifact `f76ffe60`)과 레이아웃 재편 라운드 2 목업(Artifact `5df2af11`, 라벨링 탭만 구현 완료)을
교차 점검하고, 모델/라벨링/학습/추론 4탭 전체 실제 코드(`main.py` 전역 스타일시트,
`model_tab.py`, `labeling_tab.py`, `class_panel.py`, `annotation_canvas.py`,
`image_browser.py`, `training_tab.py`, `inference_tab.py`, `overlay_viewer.py`,
`config_form.py`, `loss_chart.py`, `main_window.py`, `log_panel.py`, `settings_dialog.py`
등)를 직접 대조했다. 코드는 건드리지 않음 — 목업/문서만 산출.

**Artifact URL**: https://claude.ai/code/artifact/7876ed3e-e6ef-4d8b-92cb-cd2fecf2d98a
(기존 두 Artifact `f76ffe60`/`5df2af11`를 대체하는 최종 통합본으로 취급)

### 이미 일관적인 것 확인
- 서브스플리터 hover 패턴(`#374151`→hover `#60a5fa`)이 구현 완료된 라벨링 탭과 아직 목업
  단계인 학습/추론 탭 목업에서 완전히 동일 — 라벨링 탭 구현이 기존 메인 스플리터 스타일을
  그대로 재사용한 것이라 학습/추론 탭에 서브스플리터를 추가해도 톤이 깨지지 않음, 라운드 2
  목업 그대로 진행 가능하다고 재확인.
- `image_browser.py`의 폴더 헤더 배경 `#1a2235`/텍스트 `#60a5fa`가 레이아웃 목업이
  "재사용"이라 표기한 값과 실제 코드에서 정확히 일치 — 추론 탭 신규 트리 컴포넌트에
  안전하게 이식 가능.
- 라벨링 탭 툴바의 실제 `tool_action()` 7회 호출(polygon/brush/brush_fill/eraser/
  eraser_flood/select/pan)이 아이콘 목업이 재설계한 7종과 정확히 대응.

### 발견한 불일치 8건 (심각도 순, 상세는 Artifact 2절)
1. **(높음)** `model_tab.py` 코드 에디터·로그 패널이 `#0D1117`/`#3fb950`/`#f85149`/`#79c0ff`
   등 GitHub 다크 테마 계열의 완전히 별도 팔레트 사용 — 앱 표준(`#111418`/`#10b981`/
   `#f87171`/`#60a5fa`)과 나란히 두면 가장 먼저 눈에 띄는 이질감.
2. **(높음)** `loss_chart.py`(matplotlib)가 `#1e1e1e`/`#2d2d2d`/`#4fc3f7`/`#ef9a9a` 등
   독자 팔레트 정의 — Qt 스타일시트를 상속 못 받는 기술적 한계는 있으나, 지금 값은 앱 톤과
   가장 멀다. facecolor/텍스트만이라도 앱 토큰(`#111418`/`#1f2329`/`#e5e7eb`)에 맞추길 권장.
3. **(중간)** 라벨링 탭 툴바 — 승인된 아이콘 재설계는 도구 7종만 다루는데, 같은 툴바 안
   실행취소(↶)/전체지우기(🗑)/어노테이션표시(👁)/OK(✅)/자동라벨링(✨)/전체화면(🔳) 6개
   이모지가 그대로 남아 SVG 7개와 색이모지 6개가 한 줄에 공존 — 재설계 이전보다 더 눈에
   띄는 불일치가 될 위험. 같은 라운드에서 함께 SVG화 필요.
4. **(중간)** 텍스트 없는 아이콘 전용 버튼 5개(`main_window.py`의 🔄/📁, `log_panel.py`의
   🧹/📂, `settings_dialog.py`의 📋)가 "장식 이모지 제거"(텍스트만 남기기) 방식으로 처리되면
   버튼에 아무것도 안 남는 문제 — 기능 아이콘 카테고리로 재분류해 SVG 세트에 포함 필요.
5. **(중간)** `model_tab.py`의 텍스트 붙은 버튼 이모지 4개(📚/📂/🔎/✅)가 기존 1단계
   Explore 조사의 "장식 이모지 제거" 대상 파일 목록에서 누락됨 — 구현 시 목록에 추가 필요.
6. **(낮음)** `image_browser.py` 상태기호 실제값(`#6ddf6d`/`#5ba8ff`/`#999999`)이 승인된
   아이콘 목업 목표값(`#10b981`/`#60a5fa`)과 다름 — 새 문제 아니라 계획이 아직 미구현이라는
   사실의 재확인.
7. **(낮음)** 보조 텍스트 색상이 탭마다 `#888`(`labeling_tab.py`/`inference_tab.py`)과
   `#9ca3af`(표준 토큰)로 혼용, `inference_tab.py`는 `#ccc`도 사용 — 시각차는 미미하나
   유지보수 시 값이 흩어져 있으면 재감사가 반복됨. `#888`→`#9ca3af`, `#ccc`→`#e5e7eb` 정규화 권장.
8. **(낮음)** `training_tab.py` CUDA 배너 성공/실패 배경(`#0d1f0d`/`#1f0d0d`)이 팔레트 밖
   신규값 — 톤 자체는 자연스러우나 그룹박스 표준 배경(`#1f2329`) + 좌측 색상 보더 같은 기존
   관용구로 대체하면 새 색을 늘리지 않고 같은 효과 가능. 급하지 않음.

### 아이콘 세트 확장안
승인된 7종에 신규 12종(라벨링 툴바 잔여 6종 + 아이콘 전용 버튼 5종 + 여유 1종)을 같은 시각
언어(1.6px 스트로크, `currentColor`, 24×24)로 추가 — Artifact 3절에 스케치 포함. 새 색상
도입 없음, 활성/hover는 기존 `#60a5fa` accent 그대로.

### 학습/추론 탭 갱신 목업
기존 라운드 2 목업을 실제 `training_tab.py`/`inference_tab.py` 현재 구조(큐 박스 필드,
CUDA 배너, ETA 라벨, 3열 메트릭 테이블, 체크포인트 테이블, 오버레이 뷰어 3분할 등)에 맞춰
Before/After로 다시 그림 — Artifact 4·5절.

### 팔레트 참조표
`main.py` 핵심 10색에 실제 코드 전반에서 함께 쓰이는 보조 톤(`#e5e7eb`/`#9ca3af`/`#6b7280`/
`#1e3a5f`/`#065f46`/`#1a2235`/`#34d399`/`#252a33` 등)을 더한 확장 참조표를 Artifact 6절에
문서화 — 모두 기존 코드에 이미 쓰이던 값이라 신규 색상 도입 아님.

### 실행 순서 제안 (7단계, Artifact 7절)
1. 기능 아이콘 SVG화 확장판(7종+신규12종) — 툴바 내 이질감 해소 최우선
2. 장식 이모지 제거(파일 목록에 `model_tab.py` 추가)
3. `model_tab.py` 에디터/로그 팔레트 정규화 (발견 1)
4. `loss_chart.py` 배색 조정 (발견 2)
5. 학습 탭 큐 서브스플리터 + 추론 탭 서브스플리터·이미지 트리 (1번과 병행 가능)
6. i18n 밖 3파일 en 전환 (1~5 확정 후 마지막)
7. 팔레트 토큰 정규화(발견 7·8, 저우선 — 여유 있을 때 또는 위 라운드에 곁들임)

**상태**: 목업 완료, 사용자 승인 대기 (구현 착수 전 필수). 기존 두 Artifact(아이콘/레이아웃
라운드2)는 이 통합본으로 대체 — 다음 구현 라운드는 이 Artifact를 기준으로 진행할 것.
