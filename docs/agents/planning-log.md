# 기획 (Planning) 로그

역할 설명은 [README.md](README.md) 참고. append-only — 최신 항목이 아래에 추가된다.

---

## 2026-08-19 — 현재 상태 파악 및 워크플로우 세팅

### 확인한 프로젝트 상태
- 최신 릴리즈: `v1.6.0` (2026-05-16, docs/CHANGELOG.md) — 폴리곤 snap-to-close, RGB 채널 분리 뷰어, 픽셀 값 표시
- Phase 1~5(모델 로더/라벨링/학습/추론) 전부 완료 상태이며, 그 이후로도 성능 최적화·CUDA 진단·자동 라벨링·프로젝트 관리(`app/core/project.py`)·SAIGE 변환기 등 CLAUDE.md에 기술되지 않은 기능이 다수 추가되어 있음 (커밋 로그 기준). → `docs/roadmap.md` 신설로 정리(아래 항목 참고).
- `projects/nok/` — 실제 작업자가 사용 중인 라벨링 프로젝트. `annotations/11번.json`, `7번.json`의 uncommitted 변경은 새 코드 작업이 아니라 **정상적인 라벨링 작업물**(폴리곤 좌표 추가)이며, `classes.json` 신규 파일도 클래스 정의(0=background, 1=object)로 정상 산출물.
- `main.py` uncommitted 변경 1건: CSS 오타 발견 → [verification-log.md](verification-log.md) 참고.

### 다음 작업 후보 (사용자 확인 필요)
- 이번 세션에서 사용자로부터 구체적인 신규 기능 요청은 없었음. `main.py` 오타 수정 외에는 대기 중.
- 다음 작업 요청 시 이 파일에 범위/우선순위를 먼저 기록하고 구현으로 넘긴다.

---

## 2026-08-19 — CLAUDE.md의 "구현 단계" 표를 `docs/roadmap.md`로 이관

- CLAUDE.md 최상단에 있던 정적 Phase 1~5 표는 실제 프로젝트 상태(다수의 추가 기능)를 반영하지 못해 살아있는 문서인 [docs/roadmap.md](../roadmap.md)로 대체. CLAUDE.md에는 로드맵 문서 포인터만 남김.

---

## 2026-08-19 — 성능/버그 개선 계획 수립 (검증 결과 기반)

### 배경
검증 에이전트가 `projects/nok/` 실데이터로 전체 앱을 벤치마크해 8개 항목(버그 1 + 성능 7)을 발견 ([verification-log.md](verification-log.md) "전체 병목(Perf) 검증" 항목). 이번 기획은 이 8개 항목의 우선순위·범위·실행 순서를 정리하는 것.

### 한 일
- 스펙 문서 신설: [docs/specs/perf-improvement-plan-2026-08-19.md](../specs/perf-improvement-plan-2026-08-19.md) — 항목별 해결 방향/난이도/리스크/의존관계, 6개 라운드(R1~R6) 실행 순서.
- **BUG-002(brush_mask RLE 언더플로우, 데이터 유실)를 R1에 단독 최우선 배치**: 성능 항목과 축이 다른 심각도(되돌릴 수 없는 데이터 손실)이며, 원인이 `rle_encode()` 한 곳으로 좁혀져 다른 항목과 의존관계가 없고, R3(annotation_canvas 작업)에서 브러시 관련 검증을 하려면 먼저 고쳐져 있어야 함.
- R2(#2 임포트 지연로딩)는 `main.py` 주석에 명시된 과거 DLL 순서 이슈(BUG-001 계열) 재발 리스크가 있어 단독 라운드로 격리.
- R3(#4+#7), R6(#6+#8)은 같은 파일/같은 저리스크 성격끼리 묶어 검증 비용 절감. R4(#3), R5(#5)는 리스크·트레이드오프가 커서 단독 라운드 유지.
- #6, #8은 nok(5장)로는 실측 불가한 정적 분석 추정 — 구현 자체는 저비용/저리스크라 진행하되, R6 검증 시 더미 이미지 수백~수천 장 규모 합성 프로젝트로 재검증하도록 조건을 명시.
- `docs/roadmap.md`에 "성능/버그 개선" 절 추가(R1~R6 체크박스, 상태 "예정").
- `docs/decisions-needed.md`에 3건 등록: (1) #2 지연 임포트의 DLL 리스크 감수 여부, (2) #3 `num_workers` 새 기본값, (3) #5 추론 미리보기 다운스케일 상한.
- 코드는 건드리지 않음. `git status` 확인 완료(계획 관련 문서 3건만 변경, 기존 uncommitted 항목 그대로).

### 상태
완료 — 구현 단계로 넘길 준비됨. 다음: 리더가 사용자에게 `docs/decisions-needed.md` 3건 확인 후 R1(BUG-002)부터 구현 에이전트에 위임.

---

## 2026-08-20 — UI/UX 재편 스펙 수립 (학습·추론·라벨링 탭)

### 배경
사용자 요청: 3개 탭(학습/추론/라벨링) UI/UX를 "사용자 편의성" 관점에서 재편. 범위는 레이아웃 +
시각 스타일. 사용자가 짚은 구체 불편: "레이아웃별 움직일 수 없고 각종 정보들이 부족". Explore
에이전트가 사전에 코드 감사를 완료해둔 결과(각 탭 고정폭/고정높이 위젯 목록, 현재 표시 정보
목록, 스타일 불일치 지점)를 그대로 받아 기획.

### 한 일
- 스펙 문서 신설: [docs/specs/ui-redesign-plan-2026-08-19.md](../specs/ui-redesign-plan-2026-08-19.md)
  — 탭별(학습/추론/라벨링) 레이아웃 유연성·정보 추가·스타일 통일 항목을 표/목록으로 구체화하고
  실행 순서를 3라운드로 구성:
  - 라운드 1(저리스크, 목업 불필요): `setMaximumWidth`/`setFixedWidth` 등 숫자 제약 조정 +
    추론 탭 스플리터 hover 강조색 누락 보완.
  - 라운드 2(중리스크, 목업 필요): 라벨링 탭 좌/우 내부 서브패널, 학습 탭 큐 영역, 추론 탭
    상단 컨트롤 영역을 고정 stretch/고정높이에서 `QSplitter`로 재구조화.
  - 라운드 3(중~고리스크, 목업 필요): 정보 추가 — 추론 탭 신뢰도(confidence) 표시(최우선,
    `inference_engine.py`에 confidence 계산이 아예 없었던 명백한 갭), 처리시간/해상도 정보,
    학습 탭 현재 LR·체크포인트 리스트 보강, 라벨링 탭 줌 배율·이미지 크기 표시
    (`annotation_canvas.py`에 zoom 시그널 신규 추가 필요 — 1638줄 파일이라 diff를 시그널
    추가로만 한정하도록 명시).
- 기존 표시 정보와 대조해 이미 있는 것(학습 탭 "전체 예상 시간")은 중복 제안하지 않음 확인.
- `docs/decisions-needed.md`에 3건 등록: (1) 리사이즈 상한 구체 수치, (2) 추론 탭 신뢰도 표시
  범위(통계만 vs 히트맵까지), (3) 라벨링 탭 `ClassPanel` 내부 200px 상한 완화(범위 확장) 여부.
- `docs/roadmap.md` "UI/UX 재편" 절 갱신 — 기획 완료 체크, 라운드 1~3 체크박스 추가.
- 코드는 건드리지 않음. 시작 전/후 `git status`로 확인 — 세션 시작 시점에 이미 있던
  `app/core/dataset.py`, `app/core/trainer.py`, `app/widgets/config_form.py`,
  `docs/agents/leader-log.md`의 uncommitted 변경은 이 작업과 무관(건드리지 않음).

### 상태
완료 — 디자인/구현 단계로 넘길 준비됨. 다음: 리더가 사용자에게 `docs/decisions-needed.md`
3건 확인 후 라운드 1은 바로 구현 에이전트에, 라운드 2~3은 디자인 에이전트의 목업 확인을
거쳐 구현으로 진행.

---

## 2026-08-20 — 추가 요구사항 반영: 추론 탭 이미지 목록 검색/정렬 기능

### 배경
사용자가 위 UI/UX 재편 스펙을 확인하기 전에 추가 요구사항을 줬다: "이미지 로드하고 그랬을 때
정보량이 부족해. 폴더 sort 기능, 검색기능도 포함해줘." 확인 결과 이는 **추론 탭의 이미지
목록**(`inference_tab.py` `_img_list`, 순수 `QListWidget` IconMode)을 가리키는 것으로
판단 — 라벨링 탭 `ImageBrowser`는 검색(디바운스)+정렬 4종+폴더 그룹핑을 이미 갖췄지만
추론 탭엔 이 기능이 전혀 없다는 갭이 기존 스펙 초안(레이아웃 "폭 제약"만 다룸)에서 누락돼
있었다.

### 한 일
- `app/widgets/image_browser.py` 재확인 후 **재사용 여부를 판단**: `ImageBrowser.reload()`가
  `_project.images_dir()`를 하드코딩하는데 추론 탭은 프로젝트 밖 임의 경로를 다루는 게
  기본 워크플로우라 전제가 다름. 상단 버튼(추가/폴더추가/삭제)이 파일을 복사·삭제하는
  편집 액션이라 추론 입력 브라우징과 안 맞음. 라벨 상태 아이콘(완료/OK/미라벨)도 추론
  컨텍스트에서 의미가 약함. `ImageBrowser`는 라벨링 탭이 활발히 쓰는 공유 위젯이라 이만큼
  뜯어고치면 라벨링 탭에 회귀 리스크가 전이됨 → **재사용 기각**, 추론 탭 전용 경량 컴포넌트로
  검색(디바운스 패턴 재사용)+정렬(파일명↑↓/폴더 3종, 라벨상태 정렬은 제외)을 새로 구현하는
  쪽으로 판단.
- 폴더 그룹핑은 트리형 접기/펼치기로 만들면 현재 썸네일 아이콘 그리드 뷰를 포기해야 하는
  트레이드오프가 있어, 1차로는 "폴더" **정렬 옵션**까지만 제안하고 트리형 필요 여부는
  사용자 확인으로 넘김.
- 이 항목을 라운드 2(레이아웃 재구조화)에 배치 — 순수 상수 조정이 아니라 검색바+정렬콤보라는
  새 서브레이아웃이 추가되는 구조 변경이라 라운드 1(저리스크)이 아니라고 판단. 다만 공유
  위젯을 건드리지 않는 격리된 신규 컴포넌트라 같은 라운드의 다른 재구조화 항목보다는 회귀
  리스크가 낮다고 명시.
- 산출물 갱신: [docs/specs/ui-redesign-plan-2026-08-19.md](../specs/ui-redesign-plan-2026-08-19.md)
  (추론 탭 절에 "이미지 목록 검색/정렬/폴더 기능 부재" 서브섹션 신설 + 라운드2 항목4 +
  디자인 확인 지점 추가), [docs/decisions-needed.md](../decisions-needed.md)(4번 항목 추가:
  폴더 그룹핑 방식), [docs/roadmap.md](../roadmap.md)(UI/UX 재편 절에 2026-08-20 추가 요청
  반영 사실 기록).
- 코드는 건드리지 않음 — 이번 세션에서 Write/Edit는 문서 4건(스펙/decisions-needed/roadmap/
  본 로그)에만 사용.

### 상태
완료. 다음: 리더가 사용자에게 `docs/decisions-needed.md` 4건(레이아웃 상한 수치, 추론 탭
신뢰도 표시 범위, ClassPanel 200px 상한, 추론 탭 폴더 그룹핑 방식) 확인 후 라운드 1부터 진행.

---

## 2026-08-20 — GitHub #2 "프로젝트 내보내기 기능" VOC 2건 스코프 산정

### 배경
GitHub 이슈 [#2](https://github.com/sfeelBot/Segmentation-Tool/issues/2)에서 접수된 VOC 2건
(`QA.md` VOC 테이블 2026-08-20)의 스코프 산정 위임: ① 최근 프로젝트 클릭 시 자동 열기, ②
프로젝트 전체 내보내기/단일 파일 통합.

### 한 일
- `app/widgets/project_start_dialog.py` 전체 흐름 확인: 최근 프로젝트 목록은 이미 더블클릭
  (`itemDoubleClicked` → `_on_recent_double_clicked` → `_try_open()`)으로 한 번에 열림 —
  중간에 별도 "열기" 버튼을 또 눌러야 하는 번거로운 흐름은 없음(가설 b 기각). 싱글클릭을
  원하는지(가설 a) 단순히 몰랐는지(가설 c)는 GitHub 이슈 원문("클릭하면 자동으로 열리게")
  만으로 판별 불가 — 한국어에서 "클릭"과 "더블클릭"이 혼용되는 경우가 많아 판단을 내리지
  않고 `docs/decisions-needed.md`에 등록.
- `app/core/project.py`(`Project` 경로 구조: images/annotations/checkpoints/user_models/
  classes.json/project.json)와 `app/widgets/export_dialog.py`(기존 export는 라벨 데이터만
  JSON/YOLO/COCO로 변환, 프로젝트 패키징 아님) 확인. 추가로 `trainer.py`/`inference_engine.py`를
  교차 확인해 체크포인트의 `model_source`가 실제 소스 코드가 아니라 태그 문자열이라는 점,
  `"loaded"`(커스텀 모델)는 자동 재인스턴스화가 안 되고 사용자가 모델 탭에서 다시 로드해야
  한다는 기존 갭을 확인 — 이는 `user_models/` 포함 여부 판단(포함해도 체크포인트와 자동
  연결은 안 됨, 그래도 코드 자산 자체는 보존 가치 있음)에 반영.
- `.gitignore`가 `projects/*/checkpoints/`, `*.pt`, `*.pth`를 이미 제외 대상으로 두고 있는
  점을 "체크포인트는 무겁고 재생성 가능한 산출물" 취급 관례의 근거로 삼아 기본 미체크 제안.
  `app/widgets/image_browser.py`의 `_FolderImportWorker(QThread)` 패턴을 확인해 zip 압축도
  같은 관례(백그라운드 워커 + 진행률 시그널)로 구현 가능함을 확인 — 스파이크 불필요(신규
  라이브러리 선택이 아니라 기존 관례 재사용, `zipfile` 표준 라이브러리로 충분).
- 스펙 문서 신설: [docs/specs/voc-github-issues-2026-08-20.md](../specs/voc-github-issues-2026-08-20.md)
  — 요청 1은 판단 보류 + 재확인 경로 제시, 요청 2는 라운드 A(export, zip, 체크박스 기본값 제안)
  + 라운드 B(import, 라운드 A 완료 후 착수 — zip 구조 확정이 선행돼야 함) 2라운드로 스코프
  산정, 라운드 B의 리스크(경로 충돌·zip 검증·버전 호환성)를 라운드 A보다 명시적으로 높게 평가.
- `docs/decisions-needed.md`에 3건 등록: (1) 요청1 싱글클릭 vs 더블클릭 재확인, (2) 라운드 A
  `checkpoints/`·`user_models/` 기본 포함 여부, (3) 라운드 B(import) 착수 여부 + 이름 충돌 정책.
- `docs/roadmap.md` "GitHub 이슈 VOC" 절 갱신 — 스펙 링크 반영, 요청1/2 각각 다음 단계 명시.
- 코드는 건드리지 않음. 시작 전 `git status` 확인 — `app/core/dataset.py`, `app/core/trainer.py`,
  `app/widgets/config_form.py`, `docs/agents/leader-log.md`의 기존 uncommitted 변경은 이번
  작업과 무관(건드리지 않음). 작업 종료 시 `git status`로 스펙/decisions-needed/roadmap/본 로그
  4개 문서 변경만 추가됐음을 확인.

### 상태
완료 — 다음: 리더가 사용자에게 decisions-needed 3건 확인. 요청1은 GitHub 이슈 재확인 코멘트
후 답변에 따라 초경량 수정 또는 안내로 종료(별도 라운드 불필요). 요청2는 답변 확인 후 라운드
A(export)부터 디자인 목업 → 구현 → 검증(주요 기능 추가라 골든패스 수준 검증 요청) 순으로 진행,
라운드 B(import)는 라운드 A 완료 후 착수 여부 재확인.

---

## 2026-08-20 — GitHub 이슈 #3~#7 스코프 산정 (라운드2)

### 배경
GitHub에 2026-08-20 새로 등록된 이슈 4건(#3 브러시 도구, #4 이미지명 복사, #5 모델 탭 변경,
#6 annotation 로딩/깜빡임)의 스코프 산정 위임. 작업 도중 리더가 2건을 추가로 전달: (1) #7
"OK 이미지 기능"(라벨 있는 이미지 OK 처리 시 확인 팝업 요청), (2) 사용자가 GitHub #6 이슈
원문("깜빡임")보다 구체적으로 보고한 별도 불편 — "annotation이 많아지니까 추가하거나 화면이
바뀔 때 로딩 속도가 매우 느려져. 최적화 반드시 필요"(#6-B로 명명해 같은 절에서 함께 정리).

### 한 일
- 이슈 5건(#3~#7) 전부 코드 기준으로 실제 원인/현황을 먼저 검증한 뒤 스펙에 반영 —
  가정으로 스코프를 잡지 않음.
- **#3(브러시 크기)**: 조절 수단은 QSpinBox(툴바)+키보드 단축키(`[`/`]`/`-`/`+`) 둘뿐,
  더블클릭·우클릭은 미구현 확인. **우클릭은 이미 전역 패닝(`mousePressEvent` 581행)에
  배정돼 있어 사용자 요청과 정면 충돌** — 사용자 확인 필요 항목으로 등록. 더블클릭은
  브러시 도구에서 미사용이라 충돌 없음(단, 정확한 동작은 미정, 마찬가지로 확인 필요). 잘림
  버그는 `setFixedWidth(60)` + 전역 QSpinBox padding(main.py)으로 원인 특정 — 저리스크
  즉시 수정 가능.
- **#4(이미지명 복사)**: `settings_dialog.py`에 이미 있는 클립보드 복사 버튼 패턴
  (`svg_icon("clipboard")` + `QApplication.clipboard().setText()`)을 그대로 재사용 가능
  확인. 추론 탭은 기존 `_lbl_filename` 라벨 옆에 곁들이면 되지만, 라벨링 탭은 파일명을
  상시 표시하는 UI 자체가 없어 채널 스트립에 신규 추가 필요. patch training 질문은
  `config_form.py`(학습 기본 샘플링 = random_crop 확인) + `auto_labeler.py`
  (`_infer_to_annotations`가 패치/타일 추론 없이 전체 이미지 리사이즈 단일 추론) 교차
  확인으로 "아니오, 오토라벨은 patch 방식이 아니다"는 사실관계를 답변 완료 — 구현 불필요.
- **#5(모델 탭 구조변경)**: `win._model_tab.loaded_model`을 training_tab·labeling_tab·
  inference_tab **3곳이 교차 참조**하는 것을 grep으로 확인 — 구조 변경 시 MainWindow가
  `ModelTab` 인스턴스를 계속 들고 있어야(표시 컨테이너만 QDialog로 교체) 이 3곳이 깨지지
  않는다는 제약을 스펙에 명시. 탭 기반 UI 패러다임을 부분적으로 깨는 결정이라 **즉시 결정하지
  않고 사용자 확인 항목으로 분류**(요청대로).
- **#6(annotation 로딩/깜빡임)** — 원래 #3~#6 조사 중 원인을 `_invalidate_overlay()`
  (`annotation_canvas.py`)로 특정: `self._overlay = None`을 동기로 즉시 실행한 뒤
  재생성은 `_OverlayWorker`(QThread)로 비동기 처리해, 그 사이 paintEvent가 오버레이 없이
  그려지는 타이밍 버그(flicker, #6-A) 확인. 이미지 전환(`load_image()`)에서도 같은 함수가
  호출됨을 확인해 "채널전환+라벨링" 복합 상황이 실제로는 단일 원인임을 정리.
  - 리더가 작업 도중 전달한 추가 불편(#6-B, "개수 늘수록 느려짐")을 조사한 결과, **서로
    다른 두 곳의 O(n) 전체 재구축 패턴**을 확인: (1) `labeling_tab.py`
    `_refresh_ann_list()` — 매 편집·이미지전환마다 어노테이션 목록 `QListWidget`을
    `clear()` 후 전체 재생성(메인 스레드 동기), (2) `annotation_canvas.py`
    `_rebuild_overlay()`/`_OverlayWorker` — 매 편집·전환마다 오버레이 픽스맵을 어노테이션
    전체에 대해 처음부터 다시 그림(백그라운드 스레드지만 개수에 비례해 시간 증가). 부수로
    `_resolve_overlap_and_merge()`도 브러시 스트로크마다 `self._annotations` 전체를
    2~3회 순회하는 O(n) 구조 확인(2차 기여 요인). 두 개 모두 R1~R6 성능개선이 다룬 경로와는
    무관 — R3(이미지 디코딩 캐시)가 만든 회귀 아님.
  - #6-B는 사용자가 "반드시 필요"로 명시했고 원인도 확정됐으나 수정 난이도가 flicker(#6-A)
    보다 높아(오버레이 컴포지팅 구조 손질 가능성) 별도 라운드 권장, R6 관례대로 대규모 합성
    프로젝트 벤치마크 조건 명시.
- **#7(OK 확인 팝업)**: 리더가 위임한 "사용자 주장이 실제 코드와 일치하는지" 먼저 검증 —
  `toggle_ok()`/`set_ok()`가 `ok` 플래그만 다루고 `annotations` 배열은 건드리지 않아
  **데이터 유실은 없지만 확인 절차 자체가 없다**는 사실 확인. `_on_clear_all()`(같은 파일)이
  이미 동일한 QMessageBox 확인→실행 패턴을 쓰고 있어 신규 설계 없이 그대로 복제 가능 —
  난이도 하로 판정.
- 스펙 문서 신설: [docs/specs/voc-github-issues-round2-2026-08-20.md](../specs/voc-github-issues-round2-2026-08-20.md)
  — #3~#7 전부 코드 근거 + 제안 스코프 + 실행 순서(난이도·파일 겹침 기준 8단계) 포함.
- `docs/decisions-needed.md`에 2건 등록(#5 구조변경 진행 여부, #3 인터랙션 방식) — 나머지
  (#6-A/#6-B/#7/#4/#3-a)는 방향이 명확해 결정 없이 바로 구현 가능하다고 명시.
- `docs/roadmap.md`에 "GitHub 이슈 VOC 라운드2" 절 신설. 작업 도중 다른 프로세스가
  "디자인 톤 홀리스틱 재검토" 5단계를 검증대기→검증완료(조건부 통과, BUG-008/009/010 발견)로
  갱신한 것을 확인해 스펙 문서의 관련 서술(파일 겹침 주의)도 최신 상태로 맞춰 반영.
- 코드는 건드리지 않음 — Write/Edit는 스펙 신설 1건, `decisions-needed.md`/`roadmap.md`/
  본 로그 갱신에만 사용.

### 상태
완료 — 다음: 리더가 사용자에게 decisions-needed 2건(#5 구조변경 여부, #3 인터랙션 방식)
확인. 나머지 5개 항목(#6-B 최우선, #6-A, #7, #4, #3-a)은 결정 없이 바로 구현 에이전트에
위임 가능. #6-B는 난이도가 높아 별도 라운드로 분리 권장.

---

## 2026-08-25 — GitHub 이슈 #8·#9 스코프 산정 (라운드3)

### 배경
사용자가 GitHub 이슈 2건을 순서대로(#9 먼저, #8 다음) 처리해달라고 요청. #9는 라벨링
캔버스 줌 초점 버그, #8은 이미지 브라우저 다중선택+삭제 신규 기능 요청.

### 한 일
- **#9(버그)**: `app/widgets/annotation_canvas.py` 전체 조사. 줌 진입점이 앱 전체에서
  `wheelEvent()`(마우스 휠) 하나뿐임을 `labeling_tab.py` grep(0건 매치)으로 확인 — 드래그
  자체로 줌하는 별도 메커니즘은 존재하지 않음. `wheelEvent()`의 커서 앵커링 수식 자체는
  검산 결과 수학적으로 정확(정지 상태 휠 줌은 정상). 원인을 `_pan_active`(좌클릭 패닝
  드래그) 상태와 `wheelEvent()`의 상호작용으로 좁힘: `mousePressEvent()`가 드래그 시작
  시점에 `_pan_start_mouse`/`_pan_start_offset`을 1회만 캡처해두고 `mouseMoveEvent()`는
  매번 이 값 기준 절대 재계산을 하는데, 드래그 도중 휠이 `_zoom`/`_pan`을 갱신해도 이
  캡처값을 갱신하지 않아 다음 마우스 이동이 휠의 커서-고정 pan을 옛 값으로 덮어씀 —
  "왼쪽 클릭(드래그) 형태에서 줌인/줌아웃할 때 초점이 안 맞는다"는 원문과 정확히 일치하는
  코드 레벨 근거. Select 도구 드래그·브러시 스트로크는 이미지 좌표를 매 이벤트 재계산해
  이 결함의 영향을 받지 않음(패닝 드래그만 해당). 재현조건이 100% 확정은 아니라 사용자
  확인용 질문 3건을 스펙에 남겼으나, 근거가 충분히 강해 구현을 막을 필요는 없다고 판단—
  결정 대기 등록은 하지 않음.
- **#8(신규기능)**: `app/widgets/image_browser.py` 전수 조사 결과 **요청 3가지(다중선택/
  다중선택 후 삭제/삭제 기능)가 이미 전부 구현되어 있음**을 확인 — `_tree`가 이미
  `ExtendedSelection`(Ctrl/Shift 다중선택), `_on_delete()`가 이미 `selectedItems()` 전체를
  순회해 이미지+대응 `annotations/{stem}.json` 함께 삭제 + 확인 다이얼로그(단건/다건 요약
  문구 분기)까지 갖춤. 사용자가 위임 메시지에서 짚은 "과거 `unhashable QTreeWidgetItem`
  버그"는 `data/logs/errors.log`에서 2026-05-27자 크래시로 실물 확인했으나, 원인
  패턴(`self._item_to_path[item] = p` — QTreeWidgetItem을 dict 키로 사용)이 현재 코드
  구조(Path가 dict 키, 아이템→Path는 `item.data(0, _PATH_ROLE)` UserRole 역조회)에서
  완전히 사라져 재현 불가로 판단. `verification-log.md` 2026-08-20 항목이 "QA.md에서 별도
  추적 중"이라 서술했지만 실제 QA.md Open/Closed 어디에도 등록이 없어 문서 정합성 갭임을
  확인(코드 결함 자체는 해소된 것으로 보여 이번 라운드에서 별도 조치하지 않음). 스코프를
  "검증 우선"으로 결정 — 코드가 이미 있는데 재구현하지 않고, 실제 GUI 동작을 먼저 확인한
  뒤 문제 발견 시에만 최소 보강. 저비용 선택 갭 1건(키보드 `Delete` 단축키 없음, 버튼
  클릭만 가능)만 기록.
- 스펙 문서 신설: [docs/specs/voc-github-issues-round3-2026-08-25.md](../specs/voc-github-issues-round3-2026-08-25.md)
  — #9/#8 각각 문제정의·코드 근거·구현 대상 파일·검증 골든패스 정리.
- `docs/roadmap.md`에 "GitHub 이슈 VOC 라운드3" 절 신설(#9/#8 체크박스, 순서 명시).
- `docs/decisions-needed.md`는 갱신하지 않음 — 이번 2건 모두 결정 없이 바로 다음 단계
  (구현/검증) 진행 가능하다고 판단.
- 코드는 건드리지 않음. Write/Edit는 스펙 신설 1건, `roadmap.md`, 본 로그 갱신에만 사용.

### 상태
완료 — 다음: 리더가 순서대로 #9 먼저(구현 에이전트에 `annotation_canvas.py`
`wheelEvent()`/`mouseMoveEvent()` 패닝 분기 최소 수정 위임 → 검증), #8은 구현보다
**검증 에이전트를 먼저** 투입해 기존 다중선택+삭제 동작을 실제 GUI로 확인한 뒤 문제가
없으면 이슈를 바로 닫고, 문제가 있으면 최소 보강을 구현 에이전트에 위임.

---

## 2026-08-27 — GitHub 이슈 #12·#15·#16·#17 스코프 산정 (라운드4)

### 배경
메인 세션 담당 GitHub 이슈 4건(`feature/zone-analysis-tab` 관련 2건은 별도 세션 처리
중이라 범위 밖). #17 이미지 목록 Ctrl+C 복사, #16 내보내기 PermissionError(실사용자
크래시 로그 첨부), #15 브러시 채우기 시 기존 라벨 경계 고려, #12 "브러시는 도구일
뿐 분류 불필요"(원문 매우 짧음). 각각 범위/우선순위/영향 파일을 코드 조사 기반으로
정리.

### 한 일
- **#17**: `image_browser.py` 확인 — `_tree`(QTreeWidget)는 이미 `ExtendedSelection`
  (GitHub #8에서 확인된 그대로)이지만 `keyPressEvent`/`eventFilter` 등 키보드 단축키
  핸들러가 전혀 없음을 재확인. `installEventFilter` 방식(서브클래싱 불필요)으로 최소
  침습 구현 제안. "파일명들"은 확장자 포함으로 결정 — `_on_delete()` 확인 다이얼로그가
  이미 `p.name`(확장자 포함)으로 미리보기하는 기존 관례와 통일, 별도 사용자 확인
  불필요한 사소한 선택으로 판단.
- **#16**: 첨부된 크래시 로그가 `self._pairs`(오늘 커밋 `7dabdb5`로 이미
  `self._image_paths`로 리팩터링된 구버전 변수명)를 참조함을 확인 — MemoryError
  건과는 무관한 별개 버그(PermissionError)임을 먼저 분리. `shutil.copy2` 호출부
  (`export_dialog.py` json/yolo/coco 3곳)는 현재 HEAD에도 retry 없이 단발 호출로 남아
  있어 취약점 자체는 여전히 유효함을 확인. 원인 조사: `_image_size()`는 이미
  `with Image.open() as im`으로 안전, `dataset.py`의 학습 캐시(`_load_cached`)는
  `Image.open().convert("RGB")`가 PIL 내부적으로 `_exclusive_fp=True`일 때 `load()`
  직후 OS 핸들을 즉시 닫는다는 PIL 동작 원리를 근거로 원인에서 제외. 유일하게 발견한
  비-`with` 패턴은 `auto_labeler.py:70`(`.width`/`.height`만 접근, `.convert()` 미호출
  → 다음 루프까지 핸들이 열려있을 수 있음)이나, Windows 기본 파일 공유 모드가
  deny-none이라 자기 프로세스 핸들이 자기 자신의 copy를 막을 개연성은 낮다고 판단 —
  **1순위 원인은 외부 프로세스(백신/탐색기 미리보기/OneDrive 등 클라우드 동기화)**로
  결론, Windows 에러 메시지 자체("다른 프로세스가 파일을 사용 중")가 이를 직접
  뒷받침. 로컬 재현은 실패(OS 레벨 파일 잠금을 인위적으로 만들 방법 없음) — 정적
  분석 기반 결론임을 명시. 제안: `export_dialog.py`에 retry+backoff 헬퍼(주 수정,
  원인이 외부든 내부든 항상 유효한 방어책) + `auto_labeler.py` `with` 보강(보조,
  위생 차원, 근본원인일 개연성은 낮음). 사용자 재현조건 확인용 질문 4건을 스펙에
  남김(동기화 폴더 여부, 동시 실행 작업 여부 등) — 결정 대기 등록은 아님(구현 진행
  막을 필요 없다고 판단, GitHub #9 라운드3 관례와 동일).
- **#15**: `_fill_enclosed()`(`annotation_canvas.py`)가 오늘 커밋 `e02cf95`(시작·끝점
  강제 연결 제거) 이후에도 여전히 이번 궤적(`self._brush_np`)만으로 flood-fill함을
  확인. 핵심 발견 — **병합 자체는 이미 있다**: `_consolidate_class_region()`이 커밋
  직후 같은 클래스의 인접/겹치는 brush_mask들을 connectedComponents로 자동 병합하므로,
  이번 이슈에 필요한 신규 작업은 "벽 확장"뿐(새 병합 로직 불필요). 설계: 기존 어노테이션
  마스크를 flood-fill 입력에 OR로 추가하되, **커밋되는 새 마스크는 (이번 궤적) ∪
  (flood-fill로 새로 드러난 빈 공간)만** 남기고 기존 어노테이션이 차지하던 픽셀은
  제외 — task brief가 우려한 "기존 어노테이션이 새 annotation_id로 흡수되는 회귀"를
  원천 차단하는 설계. 성능은 `_resolve_overlap_and_merge`/`_consolidate_class_region`
  기존 bbox 스코프 패턴을 재사용해 후보 축소(bbox-vs-bbox) + 작업 캔버스를 로컬
  사각형으로 축소하는 방안 제시, 로컬 사각형 네 모서리가 전부 벽이 되는 위험(패딩
  부족 시 오탐)까지 식별해 패딩 확장 재시도 → 최종 실패 시 전체이미지 폴백을 안전장치로
  제안. **결정 대기 2건 등록**(벽 범위: 같은 클래스만 vs 모든 클래스 — 답에 따라
  구현 로직 자체가 달라짐, 패딩 마진 기본값 — 임의로 정하면 이슈가 원하는 "이어 그리기"
  거리감을 못 맞출 위험) — 구현 착수 보류 권장.
- **#12**: 원문이 매우 짧아("브러쉬는 그리는 도구일뿐 이걸 annotation에서 분류할 필요는
  없어") 3가지 가설을 코드로 검증. (a) `class_panel.py` — 클래스 목록만 다루고
  어노테이션 타입 노출 없음, 기각. (b) `labeling_tab.py` `_refresh_ann_list()`가
  `type_label = "Poly" if ann.type == "polygon" else "Mask"`로 목록 항목 텍스트에
  타입 태그를 직접 노출함을 확인 — **가장 유력한 원인**. (c)
  `_resolve_overlap_and_merge()`/`_consolidate_class_region()`이 brush_mask끼리만
  병합하고 폴리곤은 병합 대상에서 제외됨을 확인(같은 클래스라도 그리기 도구가 다르면
  항상 별개 항목) — (b)의 인상을 강화하는 배경 요인이지만 자료구조 제약상(벡터 vs
  래스터) 단독으로 없애기 어려움. 옵션A(저비용, `[Poly]`/`[Mask]` 태그만 제거) /
  옵션B(고비용, 타입 무관 자동 병합·좌표 정밀도 손실 리스크) 2안 제시. **결정 대기
  1건 등록**(원문이 정말 이 화면을 가리키는지 스크린샷으로 재확인 + 옵션A/B 선택) —
  원문만으로는 재현 조건이 100% 확정 아니라 구현 착수 보류 권장.
- 스펙 문서 신설:
  [docs/specs/voc-github-issues-round4-2026-08-27.md](../specs/voc-github-issues-round4-2026-08-27.md)
  — 4건 각각 문제정의·코드근거·설계안·결정필요사항·구현대상파일·검증골든패스 정리,
  실행순서(#16→#15→#12→#17, 파일 겹침 없어 병렬 가능) 명시.
- `docs/decisions-needed.md`에 2건 등록(#15 벽범위+패딩마진, #12 화면확인+옵션선택).
  #16·#17은 결정 없이 바로 구현 가능하다고 명시.
- `docs/roadmap.md`에 "GitHub 이슈 VOC 라운드4" 절 신설(4개 항목 체크박스, 처리순서
  명시).
- 코드는 건드리지 않음 — Write/Edit는 스펙 신설 1건, `decisions-needed.md`/
  `roadmap.md`/본 로그 갱신에만 사용. 시작 전 `git status` 확인(clean).

### 상태
완료 — 다음: 리더가 사용자에게 decisions-needed 2건(#15, #12) 확인. #16·#17은 결정
없이 바로 구현 에이전트에 위임 가능(#16 우선, #17은 언제든 병렬).

---

## 2026-08-27 — GitHub #12 옵션B 상세 설계 정정 (#15 커밋 후 재검토)

### 작업 요약
- 최신 #15 커밋 `7f33695`의 `_fill_enclosed()`를 기준으로 옵션B 설계를 코드와 재대조.
- #15가 채움 성공 후 최종 결과의 `_mask_bbox(..., margin=1)`로 `_brush_bbox`를
  갱신한다는 계약을 #12 구현 전제에 명시. #12는 원 stroke bbox가 아니라 이 최종
  bbox를 폴리곤 접촉 판정과 병합에 그대로 사용하도록 정정.
- 기존 `_rasterize_polygons_touching()`의 단순 AND는 겹침만 찾고 4-neighbor 인접을
  놓친다는 오류를 정정. bbox는 후보 축소에만 쓰고, 로컬 bbox 래스터 + cross-kernel
  1px 접촉 검사로 겹침/상하좌우 인접을 확정하며 대각선은 제외하도록 설계 확정.
- 모든 브러시 커밋에서 전체 이미지 크기 임시 폴리곤 마스크를 반복 생성하지 않도록,
  후보 판정은 로컬 배열만 사용하고 실제 변환 확정 시에만 저장용 full-size 마스크를
  1회 생성하는 성능 요구를 추가.
- 다른 클래스는 신규 타입 통합 대상이 아니며, mask-mask의 기존 나중 우선과
  polygon-mask가 함께 남을 수 있는 현행 동작을 그대로 보존한다고 정확히 기술.
- 겹침/4-neighbor/대각선/bbox-only 오탐 매트릭스, #15 최종 bbox 통합, full-image
  allocation 방지 계측을 필수 자동 테스트로 보강.

### 상태
완료 — `docs/specs/voc-github-issues-round4-2026-08-27.md`의 옵션B 설계를 구현 가능한
정확도로 정정함. 다음 단계는 #12 구현 에이전트가 해당 절의 최종 bbox·로컬 접촉 판정·
현행 다중클래스 보존 계약을 따라 `annotation_canvas.py`와 전용 테스트를 수정하는 것.
## 2026-08-25 — 존(Zone) 분석 탭 기획 (배터리 캡 녹 검사 독립 도구)

### 배경
사용자가 `feature/zone-analysis-tab` 브랜치 위에서, 기존 4탭(모델/라벨링/학습/추론)과
완전히 독립된 신규 도구를 요청. 삼성SDI 원통형 배터리 캡 검사 이미지(5472×3648 BMP)
예시를 근거로, 동심원 구역(존)별 녹 검출 면적 비율(%)을 계산하는 도구임을 확인. 5개
핵심 요구사항은 사용자가 이미 답변 완료한 상태로 전달됨(재질문 불필요) — 완전 독립(프로젝트
시스템 미사용, 체크포인트 직접 로드+즉석 추론), Hough 자동검출+수동 드래그 편집 동심원,
존별 클릭 선택+퍼센티지, 블랍 클릭 삭제+재계산, 브랜치 확정.

### 한 일
- CLAUDE.md/roadmap/planning-log 선행 확인 후, `app/core/inference_engine.py`,
  `app/main_window.py`, `app/widgets/annotation_canvas.py`(줌/팬 구조),
  `app/widgets/overlay_viewer.py`(줌/팬 QPainter 패턴), `app/core/project.py`,
  `app/core/annotation_store.py`, `app/core/model_loader.py`/`model_validator.py`,
  `app/core/trainer.py`(체크포인트 저장 스키마)를 코드 근거로 전수 조사.
- **핵심 발견 1**: CLAUDE.md 문서상 "QGraphicsView 기반"이라 적힌 `AnnotationCanvas`가
  실제로는 순수 `QWidget` + `paintEvent` 커스텀 구현임을 재확인(문서-코드 불일치, 이번
  기획 문서에 정정 기록). 신규 캔버스는 더 가벼운 `overlay_viewer.py`의 줌/팬 패턴을
  모델로 삼는 것으로 판단(1600줄짜리 `AnnotationCanvas` 전체를 상속/재사용하기엔 과함).
- **핵심 발견 2 (판단 근거)**: `load_model_from_ckpt()`는 `model_source == "preset:*"`일
  때만 완전 독립적으로 모델을 재구성하고, `"loaded"`(커스텀 아키텍처)면 체크포인트에
  소스 코드가 없어 `None`을 반환 — 기존 `inference_tab.py`는 이 경우
  `win._model_tab.loaded_model`(모델 탭 인메모리 상태)로 폴백하는데, 이는 정확히 이번
  도구가 배제해야 할 탭 간 교차 의존. 대응으로 `model_validator.validate()` +
  `model_loader.load_from_code()`(둘 다 Qt/프로젝트 의존 없는 순수 함수)를 탭 안에
  직접 내장해 코드 재입력 폴백을 구성하기로 판단, `save_user_code()`(프로젝트
  `user_models_dir()`에 쓰기)는 호출하지 않기로 명시.
- **핵심 발견 3 (판단 근거)**: `inference_engine.run()`이 내부에서 무조건
  `annotation_store.load_classes()`(현재 열린 프로젝트의 `classes.json`)를 호출함을
  확인 — 임의 경로에서 불러온 체크포인트의 클래스 정의와 우연히 열려 있는 프로젝트의
  클래스 정의가 무관할 수 있어 "독립성 원칙" 수준이 아니라 실질적 정확성 버그로 판단.
  `run()`/`run_sliding_window()`/`refilter()` 3곳에 `classes: list[ClassDef] | None = None`
  선택 인자만 추가(기존 호출부는 인자 생략 시 완전히 동일 동작이라 회귀 없음)하는 최소
  침습 수정을 제안 — 신규 탭은 추론 1회 실행 후 `raw_class_map`의 고유 클래스 id로
  `ClassDef`를 즉석 구성해 전달(프로젝트 파일 읽기 없음, 색상은 기존
  `annotation_store.DEFAULT_PALETTE` 상수 재사용, 새 팔레트 발명 없음).
- **원(circle) 데이터 모델 판단**: 공유 중심점을 데이터 모델에 강제하지 않고 각 원을
  독립 `(id, cx, cy, r)`로 저장하기로 결정 — 존 계산이 "원판 마스크 집합 차집합"으로
  정의되면 완벽한 동심 가정이 필요 없고(실측 정합 오차에도 자연히 견고), OpenCV
  `HoughCircles`의 반환 형식과도 그대로 일치해 변환 비용이 없음. "두 옵션 중 더 단순한
  쪽이 곧 더 정확한 쪽"이라 판단해 결정을 내림(스파이크 불필요 — 라이브러리 선택 문제가
  아니라 자체 기하 모델링 문제).
- 탭 배치 위치는 기존 QTabWidget 5번째 탭으로 판단(별도 진입점 아님) — "완전 독립"
  요구사항이 데이터 모델(프로젝트 시스템) 독립을 가리키는 것으로 확인되었고, 별도
  진입점은 `QApplication`/전역 스타일시트/i18n/아이콘 로더를 통째로 복제해야 해 명백한
  과잉 구현이라 판단. 근거를 스펙 문서에 남김.
- 스펙 문서 신설: [docs/specs/zone-analysis-tab-2026-08-25.md](../specs/zone-analysis-tab-2026-08-25.md)
  — 배경/확정 요구사항/판단 4건(탭 배치, 원 데이터 모델, 모델 재구성, 타겟 클래스)/
  파일 구조/데이터 흐름/UX 흐름 상세(원 편집, 존 네이밍, 블랍 삭제)/라운드 분할(4개)/
  결정 필요 항목/향후 확장 후보(체크포인트에 클래스 메타 저장 — `trainer.py` 변경이라
  이번 스코프 밖으로 명시적 제외)/리스크.
- `docs/decisions-needed.md`에 1건 등록: 타겟(녹) 클래스 2개 이상 검출 시 v1 범위(단일
  드롭다운 vs 다중 비교표) — 위 5개 확정 요구사항 재질문이 아니라 그 안의 세부 구현
  디테일임을 명시.
- `docs/roadmap.md`에 "존(Zone) 분석 탭" 절 신설 — 요구사항/판단 근거 요약, 신규 파일
  목록, 라운드 4개 체크박스(전부 착수 대기), 결정 대기 1건 링크.
- 코드는 건드리지 않음. 시작 전 `git status` 확인 — `app/core/inference_engine.py`의
  uncommitted 변경은 이 세션 시작 전부터 있던 것으로 이번 기획 작업과 무관(수정하지 않음).
  작업 종료 시 Write/Edit는 스펙 신설 1건 + `decisions-needed.md`/`roadmap.md`/본 로그
  갱신에만 사용.

### 상태
완료 — 다음: 리더가 사용자에게 `docs/decisions-needed.md` 신규 1건(타겟 클래스 다중
범위) 확인 후 R1(탭 스켈레톤+파일로드+모델재구성+추론실행)부터 구현 에이전트에 위임.
R1은 `inference_engine.py`가 기존 4탭과 공유되는 파일이라 회귀 여부를 검증 단계에서
반드시 확인하도록 명시할 것.

---

## 2026-08-25 — 존(Zone) 분석 탭 스펙 정정 (판단 2 전면 수정: 원→컨투어)

### 배경
사용자가 위 2026-08-25 존 분석 탭 기획을 채택된 실물 참고 이미지(배터리 캡 사진 위 빨간
라인 오버레이)로 직접 정정. 빨간 라인은 완벽한 수학적 원이 아니라 실제 크림핑/가스켓 링
에지를 그대로 따라가는 울퉁불퉁한 폐곡선(컨투어)임을 사용자가 확인해줌(원문: "각 3개의
원이 아니고 영역별 contour내부의 원형"). 원래 요청 문구 "동심원 형태의 엣지 여러개"를
리더가 "완벽한 원"으로 오판해 판단 2를 `(id, cx, cy, r)` 파라메트릭 원 모델로 설계했던
것이 오류였음 — 아직 구현 착수 전(코드 변경 없음)이라 스펙+로드맵만 정정. 아직 구현
착수 전이라 코드 영향 없음.

### 한 일
- **판단 2 전면 재작성**: 원 데이터 모델 `(id, cx, cy, r)`을 폐곡선 점 목록
  `Contour(id, points: list[(x,y)])`로 교체. 순서 개념(바깥→안쪽)은 반지름 대신
  `cv2.contourArea()` 기준 면적 내림차순 정렬로 유지 — 배터리 캡 구조상 경계들이 대략
  nested라서 원래 판단 2의 "반지름 오름차순 정렬"과 동일한 역할을 면적이 대신함.
- **자동 검출 방식 변경**: `cv2.HoughCircles` 단독 → `cv2.Canny`(엣지 검출) →
  `cv2.findContours(RETR_LIST)`(폐곡선 후보 추출) → 원형도(4π·area/perimeter²)+면적
  필터(링 모양만 통과) → 면적 내림차순 정렬 → `cv2.approxPolyDP`(소량 tolerance)로 점
  개수 단순화(선택적, 과도한 단순화는 실측 정밀도를 해치므로 tolerance를 작게 유지).
- **수동 편집 UX**: `app/widgets/annotation_canvas.py`를 재조사한 결과, "클릭으로 점
  추가 + 첫 점 근처 스냅으로 폐곡선 닫기"(`_poly_pts`/`_poly_snap`/`_draw_wip_polygon`
  패턴, `_c2i()`/`_i2c()` 좌표 변환)는 새 폐곡선을 처음부터 그릴 때 그대로 재사용
  가능하다고 확인. 반면 **이미 닫힌 폴리곤의 개별 정점 드래그 이동/변 클릭 삽입/정점
  삭제는 이 코드베이스 어디에도 선례가 없음**(grep 0건, Select 도구는 어노테이션 전체
  이동만 지원)을 확인해 신규 구현 필요 항목으로 명시. 범위는 "정점 드래그+추가+삭제"
  수준으로 한정(프리핸드 스플라인 에디터 등 과한 기능은 배제 — YAGNI).
- **존(zone) 계산 로직 재정의**: `cv2.fillPoly`(또는 `drawContours`+`thickness=-1`)로
  각 경계를 이진 마스크로 채운 뒤 `AND NOT`으로 고리형 존 마스크 산출. 최소 경계 내부는
  그 자체로 한 존(`Zone_center`), 최대 경계 밖(이미지 경계까지)도 기존과 동일하게 포함
  (`Zone_outside`) — 새 결정 필요 없이 기존 판단 유지. nested 전제가 실제 물리 구조상
  항상 성립함을 명시하고, 비정상 교차 입력에 대한 방지 로직은 v1 범위 밖으로 명시(YAGNI).
- **라운드 분할 재검토**: 라운드 2를 서브스텝 2개로 분할 — **2a(신규)**: Qt 없는 순수
  OpenCV 스크립트로 실제 예시 이미지(`C:\Users\Feel\Downloads\[삼성SDI]_260515_원통형배터리
  녹 검사 건\...\돔조명_20MP f,l50mm\`, 7~11번×돔/돔+동축 2조명본)에 대해 Canny 임계값·
  원형도·면적 필터·`approxPolyDP` tolerance 조합을 반복 실행해 실용적 기본 파라미터를
  확보(스파이크는 아님 — 기술 선택 자체는 이미 확정, 파라미터만 정하는 구현 준비 단계).
  실용적 파라미터를 못 찾을 경우에만 리더에게 스파이크 필요 여부 재보고하도록 명시. **2b**:
  확보한 파라미터로 `ZoneCanvas` 위젯(자동검출+수동 정점 편집) 실제 구현.
- 산출물 갱신: [docs/specs/zone-analysis-tab-2026-08-25.md](../specs/zone-analysis-tab-2026-08-25.md)
  (판단 2 전면 재작성, 파일 구조 `circle_detector.py`→`contour_detector.py` 개명, 데이터
  흐름/UX 흐름 상세/"존 계산 로직" 신설 절/라운드 분할/향후 확장 후보/리스크 절 갱신,
  용어 전면 정리 "원"→"경계(컨투어)", 판단 1·3·4는 무관해 그대로 유지),
  [docs/roadmap.md](../roadmap.md) "존(Zone) 분석 탭" 절 갱신(확정 요구사항 문구 정정,
  핵심 기술 판단 문단 재작성, 신규 파일명 갱신, R2 서브스텝 반영).
- 새 결정 대기 항목은 만들지 않음 — nested 전제·존 계산 기본값·정점 편집 범위 전부 기존
  판단 연장선에서 합리적으로 판단 가능해 사용자 확인 불필요하다고 자체 결론.
  `docs/decisions-needed.md`의 기존 1건(타겟 클래스 다중 범위)은 이번 수정과 무관해 그대로.
- 코드는 건드리지 않음(요청 범위 그대로 — 스펙+로드맵만). 시작 전 `git status` 확인 —
  `app/core/inference_engine.py`의 uncommitted 변경은 이 세션 이전부터 있던 것으로 무관
  (수정하지 않음). 작업 종료 시 Write/Edit는 스펙 갱신 + `roadmap.md` + 본 로그 갱신에만 사용.

### 상태
완료 — 다음: 리더가 사용자에게 정정된 스펙(특히 판단 2 재작성, 라운드 2 서브스텝 분할)을
공유하고, 이견 없으면 기존 계획대로 R1(탭 스켈레톤)부터 구현 착수. R2는 2a 스크립트
프로토타입을 먼저 실행해 파라미터를 확보한 뒤 2b 위젯 구현으로 진행하도록 구현 에이전트에
명시할 것.

---

## 2026-08-25 — 존(Zone) 분석 탭 스펙 2차 정정 (판단 2 재정정: 컨투어→다시 원, 단 검출은 컨투어+피팅)

### 배경
바로 위 1차 정정(원→컨투어)을 사용자가 다시 정정. 사용자 원문: "자동 검출하더라도 원형을
유지해줘. 불량 때문에 이상하게 잡으면 안되니까" — 녹(불량)이 에지 근처 픽셀을 침범하면
순수 컨투어를 그대로 경계로 쓸 경우 그 불량 튀어나온 부분까지 경계 모양에 반영되어 존
계산이 왜곡된다는 뜻. 참고 이미지의 빨간 라인도 다시 보면 "에지를 있는 그대로 따라가는
자유곡선"이 아니라 "실제 픽셀 위치에 최대한 딱 맞는 원"이 사용자의 원래 의도였음이 이번
메시지로 최종 확정. 아직 구현 착수 전(코드 변경 없음)이라 스펙+로드맵만 재정정.

### 한 일
- **판단 2 재작성(2차, 최종)**: 데이터 모델을 `Contour(id, points)`에서 다시
  `Circle(id, cx, cy, r)`로 되돌림. 단 1차 정정에서 조사한 Canny+findContours 기반
  검출 흐름은 폐기하지 않고, 파이프라인 마지막 단계로 **강건한 원피팅**을 추가하는
  형태로 합침: (1) `cv2.Canny`→`cv2.findContours(RETR_LIST)`로 폐곡선 후보 추출(유지),
  (2) 원형도(4π·area/perimeter²)+면적 필터로 링 후보 선별(유지), (3) **신규** — 선별된
  컨투어 점들에 원 피팅. `cv2.minEnclosingCircle`은 이상치(녹 튀어나온 부분) 하나에도
  반지름이 확 커질 수 있어 부적합 판정, 대신 **Kasa 대수적 최소자승 원피팅**
  (`x²+y²=Dx+Ey+F` 선형화 후 `numpy.linalg.lstsq`, `scipy` 불필요)을 채택하고 1차
  피팅 후 잔차 큰 점을 제외해 **1~2회만 재피팅**하는 단순 반복으로 이상치에 강건하게
  함(RANSAC 라이브러리 등 과공학 배제 — YAGNI). 최종 결과는 다시 독립된 `(cx,cy,r)` 목록.
- **정렬 기준 원복**: 면적 내림차순(1차 정정)에서 반지름 오름차순(원래 판단 2)으로 복귀
  — 원이므로 반지름 하나로 바로 비교 가능해 더 단순.
- **존 계산 로직 원복**: `cv2.fillPoly`(1차 정정) 대신 원 마스크(`cv2.circle(thickness=-1)`
  또는 벡터화 거리식 `(x-cx)²+(y-cy)²≤r²`)의 집합 차집합(`AND NOT`)으로 복귀 — 원 데이터가
  이미 있으므로 폴리곤 채우기보다 원 방정식이 더 단순(라더 3~4단계). 사용자가 재확인한
  "원 안에 원이 있으면 하위 원의 영역은 포함되지 않게" 요구사항은 반지름 오름차순 정렬 후
  인접쌍 차집합 방식으로 이미 일반적으로 성립함을 스펙에 명시 문장으로 추가.
- **수동 편집 UX 원복**: 1차 정정에서 "annotation_canvas.py에 선례 없어 신규 구현 필요"로
  분류했던 폴리곤 정점 드래그/삽입/삭제는 데이터 모델이 원으로 돌아오며 불필요해짐 —
  원래(1차 정정 이전)의 "원 드래그 이동/반지름 핸들 크기조절/추가삭제 버튼" 수준으로
  복귀, 구현 난이도가 크게 낮아짐(중심점 이동 + 반지름 핸들 조절 2개 조작만 필요).
- **파일명 원복**: `contour_detector.py`(1차 정정 개명) → `circle_detector.py`로 재개명,
  내부 구현은 Canny+findContours+원형도필터+강건한원피팅 파이프라인이라는 것만 스펙/
  docstring 계획에 명시(사용자 지시대로).
- **라운드 2 서브스텝(2a 헤드리스 프로토타입)은 그대로 유지** — 사용자가 명시적으로
  유지해도 좋다고 확인. 다만 목적이 재정의됨: 이제는 정점 편집 난이도 검증이 아니라
  "Canny+findContours+원형도필터+강건한 원피팅" 파라미터가 실제 예시 이미지(특히 녹이
  있는 영역)에서 실제 원형을 유지하는지(이상치 강건성) 검증하는 용도.
- 산출물 갱신: [docs/specs/zone-analysis-tab-2026-08-25.md](../specs/zone-analysis-tab-2026-08-25.md)
  (상단 정정 배너에 2차 수정 이력 이어서 추가 — 1차 수정 내용은 지우지 않고 남겨 왔다갔다한
  이력 보존, 판단 2 전면 재작성, 배경/확정요구사항/파일구조/데이터흐름/UX흐름/존계산로직/
  라운드분할/향후확장후보/리스크 절 전부 원 모델 기준으로 재조정, 판단 1·3·4·블랍삭제는
  1차·2차 수정 모두와 무관해 손대지 않음), [docs/roadmap.md](../roadmap.md) "존(Zone) 분석
  탭" 절 갱신(확정 요구사항·핵심 기술 판단 문단 재작성, 파일명 재개명 반영).
- 새 결정 대기 항목 없음 — 이번 정정은 전부 사용자가 이미 명확히 지시한 방향대로 판단.
  `docs/decisions-needed.md`의 기존 1건(타겟 클래스 다중 범위)은 이번 수정과 무관해 그대로.
- 코드는 건드리지 않음(요청 범위 그대로 — 스펙+로드맵만, 착수 전이라 회귀 위험 자체가
  없음). 작업 종료 시 Write/Edit는 스펙 갱신 + `roadmap.md` + 본 로그 갱신에만 사용.

### 상태
완료 — 다음: 리더가 사용자에게 재정정된 최종 스펙(원 모델 + 컨투어 기반 검출+강건한
원피팅 결합, 라운드 2 서브스텝 유지)을 공유하고, 이견 없으면 R1(탭 스켈레톤)부터 구현
착수. R2 착수 시 2a 스크립트 프로토타입에서 "녹이 있는 영역에서도 피팅된 원이 실제
원형을 유지하는지"를 반드시 확인하도록 구현 에이전트에 명시할 것 — 이번 2차 정정의
핵심 요구사항이라 검증 없이 2b(위젯 구현)로 넘어가면 안 됨.

---

## 2026-08-26 — 존(Zone) 분석 탭 신규 기능 3건 기획 (R1~R4 완료 이후 추가 요청)

### 배경
`feature/zone-analysis-tab` R1~R4(스펙 2026-08-25) 전부 구현+검증 완료된 상태에서
사용자가 신규 기능 3건을 요청: ① 오프라인 원 검출 테스트 팝업(체크포인트 준비와 무관하게
`circle_detector.py` 알고리즘/파라미터를 실제 이미지로 튜닝) ② 폴더 단위 이미지 가져오기 +
여러 장 일괄 처리(사용자가 "가장 중요하다"고 명시) ③ 추론 탭과 동일한 threshold(AI 신뢰도/
픽셀 크기) 조절. 작업은 별도 워크트리(`D:\segmentation model-zone-analysis-tab`)에서
수행 — 같은 경로를 다른 세션이 `main` 브랜치로 동시에 쓰고 있어 공유 시 충돌 위험.

### 한 일
- CLAUDE.md/roadmap/planning-log 선행 확인 후, 기존 구현 코드 5개 파일
  (`zone_analysis_tab.py`, `zone_canvas.py`, `circle_detector.py`, `zone_metrics.py`,
  `inference_engine.py`) 전체 정독 + 참고 대상(`inference_tab.py`의 일괄 추론/Excel
  내보내기 패턴, `inference_image_list.py`, `overlay_viewer.py`)까지 코드 근거로 조사.
- **핵심 발견(세 요청 모두 신규 검출/추론 로직 불필요)**: `circle_detector.detect_circles()`가
  이미 `sensitivity` 외에 `params: DetectParams`(Canny/원형도/면적/모폴로지/이상치제거
  13개 필드, 전부 기본값 보유)를 직접 받는 인터페이스를 갖고 있어 요청1은 이를 그대로
  노출하는 팝업 UI만 있으면 됨. `inference_engine.run/run_sliding_window/refilter`는
  R1에서 이미 `classes`/`min_confidence`/`min_pixel_size` 옵션 인자를 갖춰 요청3이
  `inference_engine.py`를 전혀 수정할 필요가 없음. `inference_image_list.py`(추론 탭
  전용)는 `app.core.project` 미참조(grep 0건 재확인) + 재귀 `rglob` 스캔 + `paths()`
  일괄처리용 API까지 이미 갖춰 요청2의 "폴더 가져오기" 신규 위젯 작성이 불필요함을
  확인 — 사용자가 지시문에서 예상한 "`inference_image_list.py` 쪽이 더 적합"이 코드로
  실증됨. `overlay_viewer.OverlayViewer.set_pixmap()`이 `InferenceResult` 전용이 아닌
  범용 API임을 확인해 `ZoneCanvas`를 팝업(요청1)에도 그대로 재사용 가능하다고 판단.
- **부수 발견(요청3 착수 시 반드시 함께 고쳐야 할 결함)**: `zone_analysis_tab.py`의
  `_on_target_changed()`/`_current_target_mask()`가 존/블랍 계산에
  `InferenceResult.raw_class_map`(threshold 적용 전 원본 argmax)을 쓰고 있음을 확인 —
  지금까지는 `refilter()` 호출 시 `min_confidence=0.0, min_pixel_size=0`을 하드코딩해와서
  `class_map == raw_class_map`이 항상 성립해 드러나지 않았을 뿐, threshold 스핀박스를
  추가하는 순간 "오버레이 화면은 threshold를 반영해 바뀌는데 존 퍼센티지 숫자는 전혀
  안 바뀌는" 눈에 띄는 버그가 됨. 최소 침습 수정(2곳, `raw_class_map`→`class_map`)을
  요청3 라운드에 포함하도록 명시 — 새 UI를 붙이기만 하고 이 근본 원인을 안 고치면
  기능이 겉보기에만 동작하는 것처럼 보일 위험이 있어 반드시 같이 처리해야 함.
- **일괄 처리(요청2) 설계 — 캐시 없이 재사용으로 "이미지별 결과 확인" 요구사항 해결**:
  20MP 이미지 폴더가 클 경우 원본 해상도 `raw_class_map`/`confidence_map`을 이미지
  개수만큼 캐싱하는 것은 메모리 위험이 커 채택하지 않음. 대신 같은 체크포인트+같은
  threshold+같은 sensitivity면 항상 같은 결과가 나오는 결정론적 특성을 이용해, 사용자가
  목록에서 이미지를 재선택하면 기존 단일 이미지 플로우로 그 자리에서 다시 계산해
  확인하는 방식으로 설계 — 신규 캐시 자료구조 없이 기존 파이프라인 재사용만으로 해결.
  블랍 클릭 삭제는 본질적으로 수동 작업이라 배치 루프에 포함하지 않기로 명시(과한
  대화상자 남발 방지, 사용자가 지시문에서 직접 경계한 지점) — 대신 개별 이미지를 열어
  기존 블랍 삭제 플로우로 확인/보정 가능함을 남겨둠.
- **Excel 내보내기(요청2 하위) 포함 여부 — 사용자가 기획에 위임한 판단**: 포함을 권장하되
  화면 테이블 표시와 완전히 분리 가능한 서브스텝(3c)으로 떼어 시간이 부족하면 생략 가능하게
  설계 — `openpyxl` 패턴(`export_blobs_to_excel`)이 이미 있어 추가 비용이 낮다는 점을 근거로
  듦(long format 채택 — 개별 자동검출 모드에서 이미지마다 존 개수가 달라질 수 있어 wide
  format은 들쭉날쭉해짐).
- 스펙 문서 신설: [docs/specs/zone-analysis-tab-features-2026-08-26.md](../specs/zone-analysis-tab-features-2026-08-26.md)
  — 코드 조사 요약/판단 A(팝업)/판단 B(threshold+root-cause)/판단 C(폴더+일괄처리, C-1~C-3
  서브섹션)/파일 구조/라운드 분할(R-A→R-B→R-C, 요청 순서가 아니라 의존관계 기준 — R-B가
  R-C의 퍼센티지 정확성 전제라 먼저 배치)/결정 필요 항목(없음)/향후 확장 후보/리스크
  (BUG-018/019 재발 방지 원칙을 신규 폴더목록↔캔버스 동기화·배치 브라우징에도 적용).
- `docs/decisions-needed.md`는 갱신하지 않음(기존 "현재 없음" 상태 유지) — 사용자가 위임한
  판단(엑셀 내보내기, 개별검출 결과확인 UX, 기준원 적용 세부구현)은 전부 스펙 문서에서
  YAGNI/재사용 기준으로 직접 판단해 명시 완료, 재질문 대상 아님.
- `docs/roadmap.md` "존(Zone) 분석 탭" 절에 "신규 기능 3건(2026-08-26 요청)" 하위 절
  신설 — 요청 3건 요약, 부수 발견(root-cause 결함), 라운드 분할(R-A/B/C) 체크박스 3개
  전부 착수 대기로 추가.
- 코드는 건드리지 않음(요청 범위 그대로 — 스펙+로드맵+본 로그만). 작업 시작 전/후 모두
  워크트리가 `feature/zone-analysis-tab` 브랜치임을 확인, `git status`로 문서 3건 변경
  외 다른 변경 없음 확인.

### 상태
완료 — 다음: 리더가 사용자에게 스펙(특히 라운드 순서가 요청 순서 1→2→3이 아니라
의존관계 기준 R-A(요청1)→R-B(요청3)→R-C(요청2)로 재배열된 점, root-cause 수정 포함
사실)을 공유하고, 이견 없으면 R-A(팝업)부터 구현 에이전트에 위임. R-B는 threshold
UI 자체보다 `raw_class_map`→`class_map` 교체가 핵심이므로 구현 에이전트에게 이 부분을
빠뜨리지 않도록 명시할 것. R-C는 스코프가 커 3a/3b/3c 서브스텝마다 검증을 끊어가며
진행하도록 지시할 것.

---

## 2026-08-26 — 승인된 UI 디자인 목업을 스펙에 반영 (레이아웃 확정)

### 배경
사용자가 신규 기능 3건(오프라인 원 검출 팝업/폴더 일괄처리/threshold 조절)의 UI 디자인
목업을 승인(Artifact `984ea900`). 코디네이터가 승인된 구조(메인 탭 좌·중·우 3분할 + 팝업
레이아웃)를 텍스트로 전달하며 `docs/specs/zone-analysis-tab-features-2026-08-26.md`
갱신을 위임 — 라운드 분할(R-A→R-B→R-C, 순서 근거)은 유지하고 레이아웃 세부사항만 각
라운드 설명에 반영하라는 지시.

### 한 일
- 스펙 문서에 신규 절 "승인된 UI 레이아웃" 추가(메인 탭 상단 툴바 구성, 좌/중/우 각
  패널 내용, 팝업 구조) — 기존 `QSplitter(canvas|side)` 2분할을 `inference_tab.py`가
  이미 쓰고 있는 `[list|center|right]` 3-way 수평 스플리터 패턴으로 확장하면 충분함을
  코드 근거로 확인(신규 레이아웃 매니저 불필요).
- **판단 A(팝업) 축소**: 이전 초안에서 제안했던 "간단모드/고급모드(`DetectParams` 13개
  필드 폼)" 양단 구성은 승인된 목업에 민감도 슬라이더 1개만 포함돼 있어 **이번 라운드
  범위에서 제외**하고 "향후 확장 후보"로 이동 — 사용자가 애초 요청한 "Canny/원형도/면적/
  원피팅 파라미터 튜닝"은 이 슬라이더 하나로 충분하다고 승인된 것으로 해석. `circle_detector.py`가
  이미 `params=` 인자를 지원하므로 후속 필요 시 core 변경 없이 팝업 UI만 확장 가능함을
  명시해둠(되돌리기 쉬운 축소).
- **판단 B(threshold) 위젯 종류 변경**: 승인된 목업이 "AI 신뢰도 슬라이더+값 표시"를
  명시해 `inference_tab.py`의 `QDoubleSpinBox` 패턴 대신 이 탭은 `QSlider`+라벨(기존
  `_sensitivity_slider`/`_lbl_sensitivity` 관례 재사용)로 구현하도록 정정. 위치도 "타겟
  클래스 행 근처"에서 "상단 툴바 한 줄"로 이동 — 로직(`_on_target_changed()` 재사용,
  root-cause 수정 등)은 변경 없음.
- **판단 C(폴더+일괄처리) 확장**: 승인된 좌측 패널 구성(폴더열기+경로표시, 상태아이콘+
  퍼센티지 배지가 붙은 목록, "1번째 이미지 원을 전체에 적용" 체크박스[기본 체크]+"▶
  선택 이미지 일괄 처리 (N장)" 버튼)을 반영하며 **`InferenceImageList`에 애디티브 API
  2건이 추가로 필요함을 새로 확인** — 이전 초안의 "수정 없이 그대로 import" 판단을
  정정:
  - `set_item_status()`/`clear_status()`(상태아이콘+배지) — 이 위젯엔 없던 기능,
    `image_browser.py`의 기존 상태기호(●/✓/○) 관례를 재사용하도록 명시(새 아이콘
    언어 발명 금지).
  - `set_multi_select()`/`selected_paths()`(다중선택) — 버튼 문구 "**선택** 이미지
    일괄 처리"가 요청 2 원문("폴더 전체 또는 **선택한 여러 장**")을 반영한 것임을
    확인해, 기존 `SingleSelection` 고정을 깨지 않으면서(옵트인) `ExtendedSelection`을
    추가 지원하도록 설계. `selected_paths()`는 선택이 비어 있으면 `paths()`(전체
    목록)를 반환하도록 해 "전체 선택" 버튼 없이도 기본값이 "폴더 전체 처리"가 되게
    함(라더: 새 UI 요소 대신 빈 선택=전체라는 관례로 단순화).
  - 두 API 모두 `inference_tab.py`(기존 호출부)는 호출하지 않으므로 회귀 없음 —
    R1의 `inference_engine.classes` 옵션 인자 추가와 동일한 안전 패턴임을 명시.
- 승인된 목업에 "▶ 추론 실행" 버튼이 명시적으로 보이지 않는 점을 발견 — 자동
  재실행 UX로 통합된 것인지 텍스트 설명만으로 확정할 수 없어, **결정을 내리지 않고
  스펙에 "구현 착수 전 Artifact 원본을 직접 확인해 확정" 항목으로 남김**(모호할 때의
  기본값은 기존 수동 트리거 유지로 명시 — 20MP 폴더 탐색 중 매 클릭마다 자동 추론되면
  느려질 위험이 있어 안전한 쪽을 폴백으로 택함).
- 라운드 분할(R-A→R-B→R-C 순서·근거)은 코디네이터 지시대로 그대로 유지, 각 라운드
  설명에 레이아웃 세부사항만 추가 — 라운드 1에 "3분할 레이아웃 뼈대(툴바+스플리터
  확장)"를 포함시켜 이후 라운드가 레이아웃을 다시 갈아엎지 않도록 조정.
- 리스크 절에 2건 추가: (1) `inference_image_list.py`가 추론 탭과 공유하는 파일이라
  애디티브 변경이라도 라운드 3a 검증에서 추론 탭 골든패스까지 함께 재확인해야 함,
  (2) 다중 선택(일괄 처리 대상)과 "현재 미리보기 중인 이미지"(단일 클릭)는 서로 다른
  개념이라 혼동하면 새로운 BUG-018/019류 혼선이 생길 수 있음 — `QTreeWidget`의
  `currentItem`/`selectedItems()` 분리로 자연스럽게 구현 가능하다는 근거와 함께 명시.
- `docs/roadmap.md` "존(Zone) 분석 탭 > 신규 기능 3건" 절에 UI 목업 승인 사실 + 애디티브
  API 필요성 재확인 사실을 한 문단 추가.
- `docs/decisions-needed.md`는 갱신하지 않음 — "▶ 추론 실행 버튼 위치" 모호성은 사용자
  결정이 필요한 사안이 아니라 구현자가 Artifact 원본(이미 승인된 시각 자료)을 다시 보면
  풀리는 사실 확인 문제라 판단, 스펙 내 "구현 시 확인 필요" 메모로 충분.
- 코드는 건드리지 않음 — Write/Edit는 스펙 갱신(`zone-analysis-tab-features-2026-08-26.md`)
  + `roadmap.md` + 본 로그 갱신에만 사용.

### 상태
완료 — 다음: 리더가 갱신된 스펙을 확인 후 R-A(팝업, 이번엔 3분할 레이아웃 뼈대 포함)부터
구현 에이전트에 위임. 구현 착수 전 담당 에이전트가 Artifact `984ea900`을 직접 열어
"▶ 추론 실행 버튼 위치/자동 재실행 여부"를 확정하도록 지시할 것.

---

## 2026-08-27 — 존(Zone) 분석 탭 신규 기능 5건 기획 (라운드3, R1~R4·R-A~R-C 완료 이후)

### 배경

`feature/zone-analysis-tab` R1~R4(2026-08-25 스펙)와 신규 기능 3건 R-A/R-B/R-C(2026-08-26
스펙)가 전부 구현+검증 통과해 push된 상태에서 사용자가 추가 5건을 요청: ① Undo(여러 단계
스택, `Ctrl+Z`+클릭 가능한 버튼) ② 브러시 지우기(신규 — 픽셀 단위로 타겟 마스크를 직접
지우는 3번째 캔버스 모드) ③ 단일 이미지 Excel 내보내기(현재는 일괄 처리 결과에만 연결됨)
④ 일괄 처리 결과 wide format(이미지×존 피벗) 뷰 ⑤ 오프라인 원 검출 팝업에서 확정한 원을
메인 탭으로 가져오는 라운드트립. 작업은 별도 워크트리(`D:\segmentation model-zone-analysis-tab`)
에서 수행(같은 경로를 다른 세션이 `main` 브랜치로 동시에 쓰고 있어 공유 시 충돌 위험).

### 한 일

- CLAUDE.md/roadmap/planning-log 선행 확인 후, 기존 구현 코드 5개 파일(`zone_analysis_tab.py`,
  `zone_canvas.py`, `circle_detect_preview_dialog.py`, `zone_batch_result_dialog.py`,
  `zone_metrics.py`) 전체 정독 + 두 기존 스펙 문서(2026-08-25, 2026-08-26) 정독 +
  재사용 후보로 명시된 `annotation_canvas.py`의 undo/브러시 구현(`_push_undo()`/`undo()`/
  `_paint_circle()`/`_paint_stroke()`/`_apply_eraser()`)을 실제로 열어 패턴을 확인.
- **핵심 발견 1(요청1 Undo)**: `ZoneCanvas`에는 undo가 전혀 없다. `annotation_canvas.py`의
  undo는 "액션 1회당 `_push_undo()` 1회 호출(드래그 중엔 호출 안 함) → 전체 `_annotations`
  deepcopy → 스택 push(30개 캡)" 구조이고, BUG-014(QA.md)는 "이 deepcopy 대상이 대형
  이미지+brush_mask 수백 개짜리 원본 해상도 배열이라 캡을 걸어도 메모리 부족이 재현된다"는
  것이지 "스택 자체가 나쁘다"는 지적이 아님을 코드로 재확인. 존 분석 탭의 undo 대상(원
  목록/블랍id 집합/신규 브러시 지우기)은 전부 가벼운 자료구조라 이 BUG-014의 원인이 애초에
  적용되지 않는다는 것이 이번 설계의 핵심 — 브러시 지우기 상태를 "지운 스트로크의
  좌표/반지름 목록"(재계산 가능한 경량 표현)으로 저장하면 원본 해상도 마스크 자체는 undo
  스택에 전혀 들어가지 않는다. 이 통찰로 **3종(원편집/블랍삭제/브러시지우기)을 분리하지
  않고 단일 통합 스택으로, 캡도 없이 무제한으로** 설계 — UX(사용자는 "방금 한 마지막
  작업"을 되돌리고 싶어하지 카테고리별 분리를 원치 않음)와 구현 난이도(분리하면 시간순
  통합 로그가 어차피 다시 필요해짐) 양쪽에서 통합이 더 단순함을 근거로 판단.
- **핵심 발견 2(요청1 세부 — 기존 취소 경로와의 상호작용)**: `zone_canvas.py`의 기존
  "짧은 드래그는 원 생성 취소, 클릭으로 간주해 존 선택 처리" 분기(`mouseReleaseEvent`
  L370-385)가 undo 도입 시 "투기적으로 push해둔 엔트리가 결국 no-op로 판명"되는 사각지대를
  만든다는 것을 코드 추적으로 발견 — 대응(취소 시 방금 push한 undo 엔트리도 함께 pop)을
  설계에 명시. 일반 원칙("조기 return 이후, 실제 상태 변화 직전에만 push")과 별개로 이
  케이스만 예외적 보정이 필요함을 별도로 문서화.
- **핵심 발견 3(요청1 — 정합성 이슈)**: 기존 `set_blob_data()`가 타겟 클래스 전환마다
  `removed_blob_ids`를 초기화하는 규칙이 이미 있음을 확인 — 이 규칙을 신규 `erase_strokes`
  에도 동일 적용하는 것에서 더 나아가, **undo 스택 전체도 이 시점에 비워야 한다**는 것을
  정합성 문제로 규명: 스냅샷이 4개 필드(원/블랍id/지우기좌표)를 통째로 복원하는 구조라,
  클래스 전환 이전 시점 스냅샷을 나중에 복원하면 그 안의 `removed_blob_ids`가 현재(새
  클래스의) `blob_labels`와 대응하지 않는 오래된 id를 가리키게 되어 조용한 정확성 버그가
  생긴다 — "단순 정책"이 아니라 "복원 시 데이터가 깨지는 것을 막는 필수 조치"로 스펙에
  명시(구현/검증 양쪽에 반드시 확인하도록 리스크 절에 별도 강조).
- **핵심 발견 4(요청2 브러시 지우기)**: `annotation_canvas._paint_circle()`/`_paint_stroke()`
  (bbox-crop 벡터화 원판 비교 + 반지름 40% 간격 선형보간)과 `zone_metrics._disk_mask()`가
  수학적으로 동일한 연산임을 확인 — `_disk_mask()`를 `disk_mask()`로 공개 전환해
  `zone_canvas.py`가 import해 쓰면 신규 스탬프 로직을 처음부터 만들 필요가 없음(라이브
  드래그 중엔 bbox-crop 증분 갱신, undo 복원 시에만 전체 재생에 이 공개 함수 사용).
  기존 "블랍 삭제 모드" 토글(`_blob_delete_mode: bool`)을 `_mode: str`(circle/blob_delete/
  brush_erase) 문자열로 통합하되 `set_blob_delete_mode()` 시그니처는 하위 호환 유지(얕은
  래퍼)로 기존 배선 코드를 건드리지 않도록 설계. 3-way 배타는 `QButtonGroup` 없이 버튼
  2개의 상호 체크해제 2줄로 충분하다고 판단(라더: 버튼 2개용 새 그룹 위젯은 과함).
- **핵심 발견 5(요청2 — 신규 성능 위험)**: 기존 원 드래그(`mouseMoveEvent`)가 이미 매
  이동마다 `circles_changed`를 emit해 `_recompute_zones()`(numpy 존 재계산)를 트리거하는
  구조임을 확인 — 원 개수가 적어(보통 2~5개) 지금까지 문제가 없었을 뿐, 브러시 스트로크는
  스탬프가 수십~수백 개 찍히는 특성상 같은 패턴을 그대로 베끼면 20MP 이미지에서 눈에 띄게
  느려질 새로운 위험이 있음을 코드 근거로 규명 — 신규 시그널 `erase_changed`를 스트로크
  종료(`mouseReleaseEvent`) 시 1회만 emit하도록 명시적으로 분리하는 설계로 대응(라이브
  시각 피드백은 매 스탬프 `self.update()`로 저렴하게 유지, 무거운 재계산만 지연).
- **핵심 발견 6(요청3 단일 Excel)**: `export_zone_percentages_to_excel(rows, out_path)`가
  이미 이미지명 인자를 rows 튜플 안에서 받는 범용 함수임을 재확인 — 신규 core 함수 없이
  이미지 1장짜리 rows만 만들면 됨. `_recompute_zones()`에서 구조화된 (존이름, 퍼센티지)
  값을 문자열로만 쓰고 버리는 것을 확인해 `_compute_zone_percentages()` 공유 헬퍼로
  추출(배치 처리 3b의 유사 로직과도 일관성 확보).
- **핵심 발견 7(요청4 wide format)**: 기존 스펙(2026-08-26)이 long format을 택한 이유
  ("개별 자동검출 모드에서 이미지마다 존 개수가 달라질 수 있음")를 재확인하고, wide
  피벗의 컬럼/공란 처리 규칙(존 이름 합집합, "중심부"→"링 N"(N 오름차순)→"바깥쪽" 고정
  정렬, 없는 셀은 공란)을 직접 확정 — 사용자가 재질문하지 말라고 명시한 항목이라 스펙에서
  근거와 함께 바로 결정. `pivot_wide_format()`을 `zone_metrics.py`(Qt 의존 없는 순수 함수)에
  두어 UI(`QTabWidget` Wide 탭)와 Excel(`zones_wide` 시트 추가) 양쪽이 공유하도록 설계 —
  `export_zone_percentages_to_excel()` 시그니처는 불변으로 유지해 요청3(단일 이미지
  내보내기)을 포함한 기존 모든 호출부가 코드 변경 없이 자동으로 wide 시트를 함께 얻게 함.
- **핵심 발견 8(요청5 라운드트립)**: `auto_label_dialog.py`(`if preview.exec(): ...`,
  L393-399)에서 "`QDialog.exec()`가 accept를 반환하면 호출부가 이미 가진/getter로 읽은
  데이터로 다음 동작을 진행"하는 기존 패턴을 확인 — `CircleDetectPreviewDialog`에 그대로
  적용. 다이얼로그 자체는 여전히 `ZoneAnalysisTab`/체크포인트/프로젝트 관련 import를
  전혀 하지 않는 완전 독립 유지("메인 탭에 적용" 버튼은 `self.accept()`만 하고, 실제
  캔버스 갱신은 호출부인 `ZoneAnalysisTab._apply_circles_from_popup()`이 수행) — 사용자
  지시("이 기능만 예외적으로 메인 탭 상태를 갱신하는 경로") 그대로. 해상도가 다른 경우는
  "다른 이미지의 원을 가져오는 게 의미 없다"고 제한하지 않고, R-C 3b가 이미 검증한
  "같은 카메라/렌즈/거리, 해상도만 다를 수 있는" 전제의 비례 스케일 안전장치를 방향만
  반대로 재사용하기로 판단(사용자가 명시한 두 옵션 중 재사용 쪽 채택, 근거: 다른 샘플
  이미지로 파라미터를 튜닝한 뒤 실제 검사 대상에 적용하는 것은 자연스러운 사용 시나리오라
  판단). 메인 탭에 이미지가 없으면 적용 자체를 차단, 기존 원이 있으면 확인 다이얼로그(#7
  "OK 처리 시 라벨 있으면 확인" 패턴 재사용) 후 덮어쓰기로 설계.
- **라운드 분할**: 요청 순서(1→5)가 아니라 의존관계·파일 겹침 기준으로 재배열 —
  R3-1(요청3, 완전독립, 최우선) → R3-2(요청4, 완전독립 + `disk_mask` 공개전환을
  R3-3을 위해 선행) → R3-3(요청2, 브러시지우기, undo 없이 먼저 안정화) → R3-4(요청1, Undo
  통합 — R3-3 의존, 브러시지우기 상태가 이미 존재해야 undo 대상에 포함 가능) → R3-5(요청5,
  라운드트립 — R3-4 이후 권장, `set_circles()`가 이미 push_undo를 갖추게 돼 적용 결과가
  공짜로 undo 가능해짐). 기존 R1~R4/R-A~R-C와 번호가 안 겹치게 `R3-1~R3-5` 접두사 부여.
- 스펙 문서 신설: [docs/specs/zone-analysis-tab-features-round3-2026-08-27.md](../specs/zone-analysis-tab-features-round3-2026-08-27.md)
  — 코드 조사 요약/판단 1~5(각 요청 1개씩)/파일 구조(신규 파일 없음, 기존 5개 파일
  애디티브 수정만)/라운드 분할(R3-1~R3-5)/결정 필요 항목(없음)/향후 확장 후보/리스크.
- `docs/decisions-needed.md` 갱신 없음 — 사용자가 지시문에서 명시적으로 "설계하라"고
  위임한 판단(Undo 통합 스택 여부, 브러시 지우기 undo 표현 방식, wide format 컬럼/공란
  처리, 라운드트립 해상도 방어 방식)은 전부 스펙 문서에서 근거와 함께 직접 결정해 명시
  완료 — 재질문 대상 아님. 기존 5개 확정 요구사항·undo 깊이(여러 단계)·wide format 여부
  자체도 재질문하지 않음(사용자 지시 그대로 준수).
- `docs/roadmap.md` "존(Zone) 분석 탭" 절에 "신규 기능 5건(2026-08-27 요청, 라운드3)"
  하위 절 신설 — 판단 1~5 요약, 라운드 분할(R3-1~R3-5) 체크박스 5개 전부 착수 대기로 추가.
- 코드는 건드리지 않음(요청 범위 그대로 — 스펙+로드맵+본 로그만). 작업 시작 전/후 모두
  워크트리 `D:\segmentation model-zone-analysis-tab`이 `feature/zone-analysis-tab`
  브랜치임을 확인, `git status`로 문서 3건(스펙 신설, roadmap 갱신, 본 로그) 변경 외
  다른 변경 없음 확인(사용자 지시대로 `D:\segmentation model`(다른 세션이 `main`으로
  동시 사용 중)이 아니라 이 워크트리에서만 작업).

### 상태
완료 — 다음: 리더가 사용자에게 라운드 순서(요청 순서 1→5가 아니라 의존관계 기준
R3-1→R3-2→R3-3→R3-4→R3-5로 재배열된 점, 신규 파일 없이 기존 5개 파일만 애디티브로
수정되는 점)를 공유하고, 이견 없으면 R3-1(단일 이미지 Excel 내보내기)부터 구현
에이전트에 위임. R3-3(브러시 지우기)은 반드시 R3-4(Undo) 이전에 완료·검증까지 끝내도록
지시할 것(Undo가 브러시 지우기의 최종 상태 형태에 의존). R3-4는 `set_blob_data()`의
undo 스택 전체 리셋(정합성 조치)을 빠뜨리지 않도록, R3-3은 스트로크 종료 시에만
`erase_changed`를 emit하는 성능 설계를 빠뜨리지 않도록 구현 에이전트에 명시적으로
강조할 것.

---

## 2026-08-27 — 존(Zone) 분석 탭 GitHub 이슈 #13·#14 기획

### 배경
`feature/zone-analysis-tab` R1~R4·R-A~R-C·R3-1~R3-5 전부 구현+검증 통과, main sync
PR #18도 병합 완료된 상태에서 사용자가 GitHub 이슈 #13("존 분석")·#14("존분석-2")
등록. 리더가 두 이슈 원문을 읽고 요약해 위임(한국어, 개발자가 아닌 어투로 짧게
적힌 원문 — 요약과 함께 코드 위치 힌트 3건도 함께 전달).

### 한 일
- **원문 미확인 사실 명시**: 이번 세션에는 `gh` CLI/셸 실행 도구가 지급되지 않아
  `gh issue view 13`/`gh issue view 14`를 직접 실행할 수 없었다. 리더가 프롬프트에
  포함한 요약을 그대로 채택하되, 언급된 코드 위치(`zone_canvas.py`
  `contextMenuEvent()`/`mousePressEvent()`, `zone_analysis_tab.py`
  `_on_list_image_selected()`/"픽셀크기:" 라벨)를 전부 직접 열어 사실관계를
  교차검증 완료 — 리더 서술과 코드가 정확히 일치함을 확인. 스펙 문서 최상단에
  이 사실을 명시적으로 남기고, 리더에게 원문 재확인을 권장하는 문구를 포함시킴.
- CLAUDE.md/roadmap/planning-log 선행 확인 후, 기존 구현 코드 4개 파일
  (`zone_canvas.py`, `zone_analysis_tab.py`, `overlay_viewer.py`,
  `circle_detect_preview_dialog.py`) 전체/발췌 정독.
- **#13 요구사항1(우클릭 지름 변경)**: `contextMenuEvent()`가 `"원 삭제"` 액션
  1개뿐임을 확인. `QInputDialog.getDouble()`로 **지름**(원문 표현 그대로) 값을
  입력받아 `r = diameter/2`로 환산해 저장하는 안으로 판단 — 데이터 모델(`_CircleItem.r`,
  반지름 저장)은 그대로 두고 입력 UI 레이어에서만 지름↔반지름 변환.
- **#13 요구사항2(신규 원 중심 공유)**: `mouseMoveEvent()`의 `"create"` 분기가
  이미 `item.cx, item.cy`(저장된 중심) 기준 화면 거리로 반지름을 계산하는 구조임을
  확인해, `mousePressEvent()`의 빈 공간 클릭(else) 분기 하나만 수정하면 드래그
  반지름 로직은 코드 변경 없이 그대로 재사용 가능함을 규명(diff 최소화). **대표
  중심 산출 규칙**은 "기존 원들의 평균 중심"을 기본값으로 제안 — "가장 최근에
  추가된 원"보다 이상치(사용자가 실수로 어긋나게 옮긴 원 1개) 1개에 덜 흔들리는
  것을 근거로 삼음(정상 케이스=동심원이면 두 규칙이 결과적으로 동일해 선택
  자체가 중요하지 않다는 점도 함께 명시).
- **#13 요구사항3(양쪽 적용)**: `ZoneCanvas`가 메인 탭(`zone_analysis_tab.py`
  L271)과 오프라인 팝업(`circle_detect_preview_dialog.py` L86) 양쪽에서 동일
  클래스로 인스턴스화되는 공유 위젯임을 재확인 — 요구사항1·2를 `zone_canvas.py`에
  구현하면 신규 배선 없이 자동으로 양쪽 적용됨(리더 사전 판단이 코드로 실증됨).
- **#13 요구사항4(가장 모호한 항목)**: `_on_batch_process()`의 "1번째 이미지 원을
  전체에 적용" 분기와 `_apply_circles_from_popup()`(R3-5)의 해상도 방어 분기를
  나란히 대조한 결과, **수학적으로 완전히 동일한 비례 스케일 연산**(방향만 반대)이
  두 곳에 독립 구현돼 있음을 확인 — 리더의 "두 곳에 독립 구현돼 있을 것"이라는
  가설이 코드로 실증됨(해석 A 근거). 다만 코드를 대조하는 과정에서 리더의 해석과
  다른 대안 해석(해석 B)도 발견 — "고정 원 옵션... 오프라인 원 검출 테스트
  이미지와 동일하게"가 "로직 통합"이 아니라 "고정 원의 출처를 오프라인 팝업
  결과로 삼게 해달라"는 뜻일 수 있고, 이 경우 R3-5(같은 날 이미 완료)가 이미
  "팝업에서 원 확정 → 메인 탭에 적용 → 그 원으로 배치처리"라는 흐름을 코드
  변경 없이 지원하므로 **신규 구현이 전혀 필요 없고 검증만으로 충분**할 수
  있음을 규명. 두 해석 모두 저비용이라 스파이크는 불필요하다고 판단했으나,
  구현 방향이 서로 다르므로(A=리팩터링 코드변경, B=검증만) **사용자 확인 없이
  진행하지 않기로 결정** — `docs/decisions-needed.md`에 두 해석과 근거를 함께
  등록, 가능하면 리더가 GitHub 이슈 #13 원문을 재확인해줄 것을 권장하는 문구
  포함.
- **#14 요구사항1(원본 이미지 미리보기)**: `_on_list_image_selected()`가 이미지
  크기만 읽고 캔버스 표시 로직이 전혀 없음을 재확인(리더 서술과 일치). 결정적
  선례로 `circle_detect_preview_dialog._on_open_image()`가 정확히 같은 요구(추론
  전 순수 원본 이미지 표시)를 이미 `_rgb_to_qpixmap()`(순수 변환 헬퍼) +
  `set_pixmap()`(오버레이 전용이 아닌 범용 QPixmap 세터, `overlay_viewer.py`
  코드로 확인) + `_PREVIEW_MAX_DIM=2048` 다운스케일 조합으로 구현해둔 것을 발견
  — 그대로 이식하는 것으로 판단, 신규 렌더링 경로 불필요(사용자 지시와 정확히
  일치). `_rgb_to_qpixmap`은 이 코드베이스가 `auto_label_preview_dialog._pil_to_qpixmap()`
  와 `circle_detect_preview_dialog._rgb_to_qpixmap()`처럼 이미 같은 5줄짜리 변환
  함수를 공유 유틸 없이 독립 중복해온 기존 관례가 있어, 세 번째 중복도 그대로
  따르는 쪽을 권장(라더: 5줄짜리 순수 함수를 위해 신규 공유 모듈은 과함).
- **#14 요구사항2(라벨 텍스트)**: "픽셀크기:" 라벨 위치(L126) 확인, 1줄 변경으로
  #14 요구사항1과 같은 라운드에 포함(사용자 지시대로).
- **라운드 분할**: 파일이 겹치지 않는 두 그룹으로 나눠 병렬 진행 가능하게 설계 —
  R13-A(`zone_canvas.py` 단독, #13 요구사항1·2, 결정 없이 바로 착수 가능)/
  R14-A(`zone_analysis_tab.py` 단독, #14 요구사항1·2, 결정 없이 바로 착수 가능)/
  R13-B(대기, #13 요구사항4 — 해석 A/B 확정 후 착수, `zone_analysis_tab.py`가
  R14-A와 겹치므로 그쪽 완료 후 착수 권장).
- 스펙 문서 신설: [docs/specs/zone-analysis-tab-github-issues-13-14-2026-08-27.md](../specs/zone-analysis-tab-github-issues-13-14-2026-08-27.md)
  — 코드 근거/판단 1~6/파일 구조/라운드 분할/결정 필요 항목/리스크.
- `docs/decisions-needed.md`에 2건 등록: (1) 판단 2 대표 중심 산출 규칙(평균 기본값
  제안), (2) 판단 4 해석 A/B 확정(원문 재확인 권장).
- `docs/roadmap.md` "존(Zone) 분석 탭" 절에 "GitHub 이슈 #13·#14(2026-08-27 접수)"
  하위 절 신설 — 요구사항 요약, 라운드 분할(R13-A/R14-A/R13-B) 체크박스 3개 추가.
- 코드는 건드리지 않음(요청 범위 그대로 — 스펙+decisions-needed+roadmap+본 로그만).
  작업 시작 전/후 모두 워크트리 `D:\segmentation model-zone-analysis-tab`이
  `feature/zone-analysis-tab` 브랜치임을 확인(사용자 지시대로 `D:\segmentation model`이
  아닌 이 워크트리에서만 작업), `git status`로 문서 4건 변경 외 다른 변경 없음 확인.

### 상태
완료 — 다음: 리더가 사용자에게 (1) R13-A·R14-A는 결정 없이 바로 구현 에이전트에
병렬 위임 가능하다는 점, (2) R13-B(#13 요구사항4)는 해석 A/B 중 확정이 필요하며
가능하면 GitHub 이슈 #13 원문을 직접 재확인해줄 것을 공유. R13-A 구현 시
`contextMenuEvent()`/`mousePressEvent()`가 이미 여러 라운드(R2/R4/R3-3/R3-4)에
걸쳐 수정돼온 함수이니 기존 모드 배타 처리(원편집/블랍삭제/브러시지우기)와
충돌하지 않는지 확인하도록 명시할 것(스펙 "리스크" 절 — 실제로는 두 판단 모두
`circle` 기본 모드 범위 내 동작이라 충돌 없음을 기획 단계에서 이미 확인해뒀으나
구현자가 재확인하도록 안내).
